# 第2回LCF 事前マッチング・自己申告GMV 実装検証

## 実装対象

第2回LCF（`2026-02`、2026年12月8日・9日）について、前回の公式LINEとスプレッドシートによる受付・転記・手計算を、LCF/LCM共通アカウント内の事前マッチング、証憑付き自己申告、運営確認、調整履歴、自動集計へ置き換えた。

| 対象 | 実装結果 |
|---|---|
| ライブコマーサー | 第2回申込が`confirmed`の共通アカウントだけ利用可能 |
| 出展企業・ブランド | 第2回企業申込が`confirmed`、LCMブランドメンバーが`active`、ブランドと商品が`published`の場合だけ対象 |
| マッチング | 商品ID・ブランドID・開催回・申込ID・アカウントIDを固定し、承認前は連絡先非表示 |
| GMV提出 | 12月8日または9日、自己申告額、任意の注文数・HTTPS配信URL・備考、JPEG/PNG/WebP証憑を提出 |
| 証憑 | 8MB以下、マジックバイト検証、SHA-256、private object key、1時間の署名URL |
| 運営確認 | 同額承認、理由付き金額修正承認、理由必須の差戻し・無効化 |
| 調整 | 確認済み報告だけに符号付き調整を追加。元報告を削除せず、最終額を0円未満にしない |
| 集計 | 確認済みだけを日別、2日間累計、配信者別、ブランド別、商品別へ反映 |
| 公開範囲 | ランキングは非公開。管理画面の内部集計だけ |

## セキュリティと監査

進行中の同一商品マッチング、同日・同一配信URL、有効な同一証憑は、事前照合に加えてDB一意キーで並行送信も防止する。証憑のprivate keyとSHA-256は通常ユーザーの一覧APIへ返さない。ブランド側へライブコマーサーのメールを承認前に返さず、相互同意済みの承認後だけ連絡先を表示する。

差戻し再提出では元報告を`voided`として保持し、新しい報告へ`supersedesReportId`を保存する。承認、金額修正、差戻し、無効化、返金等調整、証憑閲覧は監査ログへ記録する。APIレスポンスには`no-store, private`を適用する。

## UI

`/lcf/mypage`には、利用資格がある場合だけ「出展商品マッチング・GMV報告」を表示する。ライブコマーサーは第2回出展対象商品から依頼し、承認済みマッチングだけをGMV報告へ選択できる。ブランド担当者は承認、追加確認、見送りを行う。

`/lcf/admin?tab=gmv`には、確認待ち・差戻し・確認済み件数、日別・累計GMV、報告一覧、クリック時証憑取得、審査、調整履歴、マッチング監査、配信者別・ブランド別・商品別の内部集計を表示する。

## 自動化しない範囲

TikTok Shop APIが未発行のため、TikTok売上の自動取得は行わない。OCRも実装せず、画像から読んだ値を自動確定しない。本人の自己申告額と運営確認額を分け、`verified`の報告だけを集計する。

## 検証結果

| 検証 | 結果 |
|---|---|
| 専用回帰 | `server/lcf-matching-gmv-reporting.test.ts` 10件成功 |
| 管理URL・マイページ回帰 | 4件成功 |
| LCF/LCM関連回帰 | 38ファイル・269件成功 |
| 差分TypeScript | 新規スキーマ、サービス、router、マイページ、管理パネルの新規エラー0件 |
| 全体TypeScript | 既存の`server/routers.ts`等の既知診断によりexit 2。今回差分とは分離済み |
| production build | Viteクライアント、esbuildサーバーの成果物生成成功。ローカルDB未起動のため末尾マイグレーションは`ECONNREFUSED`後に既存仕様どおり継続 |
| 全体Vitest | DB依存テストはローカルDB未起動のため63ファイル失敗。今回対象のLCF/LCM関連38ファイルは全成功 |

本番確認では、実データへの提出・承認・調整を行わず、GET-onlyで空状態、URL、認証、HTTP、既存第1回・LCM導線を確認する。

## 本番GET-only確認

機能コミット`250b4e20`のRailwayデプロイ成功後、管理者セッションで`/lcf/admin?tab=gmv`を開いた。初回の新規テーブル作成とデータ取得が完了し、エラーなく次を確認した。

| 確認項目 | 結果 |
|---|---|
| URL連動 | `tab=gmv`で「マッチング・GMV」タブが選択状態 |
| 第2回空状態 | 確認待ち・差戻し・確認済み、12月8日・9日、2日間累計がすべて0 |
| GMV報告 | 「条件に該当するGMV報告はありません」を表示 |
| マッチング | 全依頼・承認済み・要対応が0、監査一覧も空状態 |
| 内部集計 | 配信者別・ブランド別・商品別が空状態。公開ランキングではない旨を表示 |
| 誤認防止 | 「TikTok API自動取得ではありません」「確認済みの自己申告GMVだけを集計」を表示 |

同じLCF管理者セッションで`/lcf/mypage`を開き、`/lcf/admin`へ自動転送されず通常の共通マイページが表示されることも確認した。第2回の2026年12月8日・9日／浜松町館2階、役割別申込CTA、参加履歴アーカイブに加え、第1回の2026年9月8日・9日／八芳園のイベント詳細が維持されている。

`/lcf/admin?tab=accounts`ではアカウントタブがURLから選択され、「新規登録順」の並べ替えと既存管理項目が表示された。`/lcm`では正式公開商品1件と第1回特集29件が分離表示され、共通マイページ、サンプル、ブランド管理、ライブコマーサー一覧の導線が維持されている。

本番の実申込、マッチング、GMV報告、証憑、承認、調整への書込みは行っていない。
