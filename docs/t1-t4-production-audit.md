# LCF T1～T4下线：生产只读审计

## 2026-09-01 初始状态

已在现有登录会话中只读打开`https://www.livecommercefestival.com/lcf/mypage`。生产MyPage正常返回并显示「LIVE配信ブース 予約」区块、现行预约规则和预约按钮；本次检查没有点击预约、取消、修改或发送任何请求。

当前服务端`BOOTH_IDS`和两个预约页面仍把`T1`、`T2`、`T3`、`T4`与`T13`～`T24`共同列为可预约ブース。后台QR生成同样遍历该服务端清单。因此下线必须同时修改服务端唯一白名单、MyPage、公开预约页、后台QR/统计契约及测试，不能只把四列从一个页面隐藏。

Railway控制台的现有登录会话可用，GitHub Deployment记录确认`lcjagent / production`持续由main自动部署。开发期间远端main继续新增财务与会员提交，故提交前再次`fetch`并以fast-forward同步到`ffe3d3144b61bdc977032372673da3fa553b7677`，本次T1～T4修改以该精确提交为最终基线；恢复改动后没有合并冲突。

Railway外部控制台`https://railway.com/dashboard`已只读打开，项目搜索可以定位`lcjagent`。GitHub Deployments中最新生产记录为环境`lcjagent / production`，提交`0456fcc2a07aed02f7cfa213641f4a4fa2f4819c`，创建时间为2026-09-01 09:08:46 UTC；对应main检查结论为success。本次尚未修改Railway变量、服务或数据库。

Railway项目`lcjagent`的production环境包含在线MySQL服务与`lcjgent`应用服务，生产域名为`www.livecommercefestival.com`。只读检查时应用处于Online并有新的Building任务；后续提交前必须再次同步main，并在本次部署后单独确认构建成功和错误日志为空。

Railway MySQL数据页面确认生产数据库Deployment Online，且控制台具备连接该数据库所需的内部变量；外部沙箱中的`DATABASE_URL`仅指向未运行的`localhost:3306`，不能作为生产连接使用。当前仍未通过Railway控制台执行SQL或修改数据。

已通过Railway内置SQL编辑器执行只读聚合查询，只返回ブース、状态、预约数和去重收件人数，不读取个人明细。第一页显示：T1有4条confirmed（3个去重邮箱）及1条历史cancelled；T2有2条confirmed（1个去重邮箱）及2条历史cancelled；T3有3条confirmed（2个去重邮箱）。结果共8个分组、分2页，下一步读取第二页后再计算完整受影响人数。

完整只读结果显示：T3另有1条历史cancelled，T4有1条confirmed及1条历史cancelled。跨T1～T4合计为**16条有效预约、9个唯一受影响收件人、23条全部历史记录、10条活动槽**。通知应按有效预约邮箱去重，仅发送9封；既有历史取消记录不重复通知，16条有效预约需要统一取消并保留审计记录。

Railway MySQL在本次操作前没有既有Volume Backup。已从Backups页启动整库备份；界面状态为`Creating backup`。预约表、活动槽、审计表和邮件均尚未修改，必须等待该备份显示成功后才能进入取消流程。

取消前整库备份已完成：Railway Backups页显示`2026-09-01 09:20`、容量`1.47 GB`并提供Restore入口。该备份作为整库回滚层；下一步仍会由应用级流程对16条有效预约、10条活动槽及9个去重通知对象建立加密明细备份和审计记录，避免必须恢复整库才能追溯本次操作。

本文件不记录登录邮箱、票券ID、预约者姓名或其他个人信息。

## 2026-09-01 部署前实现与验证

服务端唯一可预约ブース清单已收敛为T13～T24。旧客户端、直接API和旧QR对T1～T4的创建、上下文查询与签到均返回明确下线错误；MyPage、独立预约页、旧QR入口和后台QR生成均与12个有效ブース一致。后台新增受控操作面板，必须输入`T1-T4`才能执行。

批量操作使用MySQL命名锁和同一事务，在取消前把16条有效预约与10个活动槽加密为AES-256-GCM应用快照并完成解密往返校验；随后把预约标记为`cancelled`、删除活动槽、逐条写预约审计，并为9个唯一收件人建立只含HMAC哈希与域名的邮件日志。邮件复用现有阿里企业邮箱优先/Gmail备用链；已被供应商接受的收件人不会重复发送，失败项可安全重试。

4个专项测试文件共25项通过；新增契约覆盖12个有效ブース、旧T1 QR拒绝、全页面文案、加密快照、事务取消、审计、收件人哈希和邮件幂等。生产构建成功。完整`pnpm check`仍被仓库本次修改前已存在的Influencer BD类型错误阻断，本次T1～T4文件没有出现在TypeScript错误清单中；构建仅保留既有`sharp`命名空间警告和沙箱无生产数据库导致的可忽略迁移连接提示。

截至本段记录，生产整库备份已经完成，但16条有效预约、10条活动槽和9封通知尚未修改或发送。下一步必须先推送GitHub并确认Railway生产部署成功，再从已部署后台执行一次幂等取消与通知。
