# LCF申込管理：画面内メール・履歴表示・同期高速化 検証記録

## 対象

LCF管理画面 `https://www.livecommercefestival.com/lcf/admin?tab=applications` の企業、ライブコマーサー、一般参加の申込カードから行うメール対応を対象とした。

## ユーザー要望

従来の「LCFメール作成」は `lcjmall.com/master/recruitment?tab=email` へ遷移していたため、LCF申込管理の同じ画面でポップアップを開き、相手との送受信履歴を確認しながら返信・新規送信を行えるようにする。また、メール画面の長い読込を改善し、送信元は `LCF@livecommercejapan.jp` とする。数字だけの件名等によりAlibaba Mailから `ESO_LOCAL_SPAM` で拒否された事例を踏まえ、同じ送信失敗を防ぐ。

## 実装

| 項目 | 実装内容 |
|---|---|
| 画面内ポップアップ | 各申込カードの「LCFメール」で `LcfApplicationEmailDialog` を開く。別サイトへ遷移しない。PCは最大1100pxで履歴と作成欄を横並び、狭い画面は縦並び。 |
| 相手別履歴 | 申込ID・開催回・申込種別からサーバー側で対象者とメールアドレスを解決する。受信・送信をアドレス指定で検索し、DB保存済み送信ログとIMAP履歴を日時順に統合・重複排除する。 |
| 初回表示 | ポップアップと保存済み履歴を先に表示し、IMAP同期は別リクエストで実行する。初回はINBOX/Sentを独立接続で並列取得し、各7秒の時間上限を設ける。 |
| 手動更新 | 初回同期が時間上限になった場合は「更新」を表示する。手動更新は単一IMAP接続でINBOX→Sentを順に取得し、最大20秒まで完全取得を試みる。 |
| キャッシュ | 相手別履歴を5分キャッシュし、同じ相手の再表示を高速化する。期限切れキャッシュは清掃する。 |
| 送信 | 同じポップアップから新規送信と受信メールへの返信を行える。CC、In-Reply-To、Referencesに対応する。 |
| 送信元 | 表示FromとReply-Toは `LIVE COMMERCE FESTIVAL <LCF@livecommercejapan.jp>`。SMTP認証とenvelope senderは既存の認証済みメールアカウントを維持し、SPF/DKIM整合を壊さない。 |
| 迷惑メール対策 | 数字だけ・同一文字だけ・6文字未満の件名、40文字未満・プレースホルダー・実質文字不足の本文を送信前に拒否する。LCF正式署名を自動付与する。 |
| 重複送信防止 | SMTP受付後に履歴保存だけが失敗した場合も送信成功として扱い、「再送しない」案内を返す。履歴保存失敗を送信失敗と誤認して同じメールを二重送信しない。 |
| 監査 | 送信はLCF管理者専用API経由とし、`send_lcf_email` として操作履歴へ記録する。 |

## 回帰・ビルド

| 検証 | 結果 |
|---|---|
| LCFメール専用回帰 | 2 files / 12 tests 成功 |
| LCF・LCM関連回帰 | 全件成功 |
| Festival・メール認証・メールフェイルオーバー回帰 | 全件成功 |
| Production build | 成功。Vite・Express成果物生成済み |
| Migration | ローカルDB未起動のため既存 `ECONNREFUSED`。既存スクリプトが `Continuing despite error...` で継続し、成果物は正常生成 |
| TypeScript | 全体には既存診断が残るが、新規 `server/lcfAdminEmailService.ts` に診断なし。変更済み既存ファイルの検出分は今回行と無関係な既存負債 |

## 本番検証

最終機能SHAは `1fe8bed2561d45f28f3dad482039ef492220e3a1`。GitHub Check成功、Railway deployment `6536117518`（production）成功を確認した。

本番の第2回ライブコマーサー申込を読み取り中心で使用し、以下を確認した。実メール送信、申込データ変更、会員データ変更は行っていない。

| 本番確認 | 結果 |
|---|---|
| ポップアップ初回表示 | 75ms |
| 初回同期の状態確定 | 8.31秒。時間上限時は画面を止めず、保存済み履歴と「更新」案内を表示 |
| 手動更新 | 14.804秒で完了し、既存の「第2回 LCF 入場チケット（QRコード）のご案内」送信履歴1件を表示 |
| PCダイアログ | 表示幅1100px、内部 `scrollWidth=clientWidth` で横切れなし |
| ページ横幅 | `scrollWidth=innerWidth=1280` で横溢れなし |
| 送信元表示 | `LIVE COMMERCE FESTIVAL <LCF@livecommercejapan.jp>` を確認 |
| 送信前ガード | 「数字だけの件名や短すぎる本文は送信できません」を確認 |
| 新規送信 | 宛先・CC・件名・本文・送信ボタンを確認。無断送信は行っていない |

## コミット

| SHA | 内容 |
|---|---|
| `c4738282` | LCF管理画面内メール会話ポップアップ、履歴API、送信API |
| `67c252c8` | SMTP受付後の履歴保存失敗による重複再送を防止 |
| `806c342d` | PCダイアログ1100px化、初回同期軽量化 |
| `1fbfeec0` | 初回INBOX/Sent並列同期 |
| `100983aa` | 初回同期の7秒時間上限 |
| `11a388b2` | 手動更新用20秒枠 |
| `1fe8bed2` | 初回並列・手動単一接続の確実な同期へ最終調整 |

## 結論

LCF申込管理から外部メールページへ移動せず、**相手別のメール履歴確認、新規メール、返信、CC、手動同期、監査**を一つのポップアップで行える。初回表示は即時、遅いメールサーバーにより画面全体が長時間ローディングする状態は解消した。迷惑メール判定を誘発した短文・数字だけのメールは送信前に拒否し、LCF正式署名と認証済みSMTP envelopeを維持する。
