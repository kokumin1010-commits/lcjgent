# 财务密码门禁视觉回归记录

## 锁定状态

财务页面在未解锁时只显示居中的“财务管理密码验证”卡片、密码输入框和“验证并进入”按钮。财务KPI、发票、现金流、合同与上传操作均未挂载或显示，未发现任何财务数据泄露。侧边栏与现有后台布局保持不变，中文字体、间距和焦点状态清晰。

## 解锁状态

正确密码后，原有“ファイナンス管理”页面与`請求書管理`标签完整显示。月度筛选、数据上传、CAP设置、发票统计、筛选与列表布局均无错位；右上角新增“重新锁定”按钮，位置明确且不遮挡原操作。测试数据为空时仍显示原有真实空状态，没有生成或修改任何财务记录。

## 交互结果

Chromium mock回归确认错误密码被拒绝，正确密码仅解锁一次，整页刷新后保持解锁，主动重新锁定后立即回到密码卡片。解锁前财务数据请求为0，业务mutation为0，非预期console、page和request错误均为0。
