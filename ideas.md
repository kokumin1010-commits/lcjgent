# ブランド司令塔移动端适配设计基准

## 参考即最终规范

线上页面 `https://lcjmall.com/master/brands` 与用户提供的手机截图是本次改造的视觉基准。现有深海军蓝背景、彩色 KPI 卡片、红色主操作、蓝色飞书同步、橙色招商入口和深色品牌卡片应继续保留；本次不进行品牌重做，只修复竖屏信息架构、尺寸和换行。

## 选定设计方向：纵向指挥台

**设计运动：** 响应式信息仪表盘（Responsive Information Dashboard）。手机端不缩小桌面版，而是把同一套功能重排为清晰的纵向操作流。

**核心原则：** 第一，页面宽度始终受视口约束，不允许根级横向滚动；第二，标题、主操作、筛选和数据按使用优先级依次排布；第三，金额与品牌名称可以压缩或合理换行，但按钮和状态不能被截断；第四，桌面端保持现有多列效率。

**色彩理念：** 延续现有低亮度海军蓝底色，让白色信息与红、蓝、橙、绿等功能色保持明确语义。移动端仅调整对比、间距和容器层级，不引入新的主色。

**布局范式：** 375–430px 使用纵向分区。返回按钮单独占据稳定区域；标题与图标并排；四个主要操作采用两列网格；KPI 保持两列但允许最后一张自然占位；筛选器先两列下拉，再整行搜索；品牌卡片内部改为可换行头部。768px 以上逐步恢复横向工具栏和多列卡片。

**标志性元素：** 保留渐变功能按钮、彩色描边 KPI 卡片与深色半透明面板三项核心视觉语言。

**交互理念：** 手机端所有核心按钮至少 44px 高，按压反馈清晰；不把核心功能隐藏进菜单，不要求用户横向滑动寻找操作。

**动画：** 沿用短促的颜色、阴影和按压缩放过渡；不增加大范围位移动画，并尊重减少动态效果设置。

**字体系统：** 继续使用 Noto Sans JP。移动端标题采用 20px/700，区块标签采用 12–14px/500–600，金额采用 20–24px/700；长名称允许截断但必须提供完整 title。

**品牌本质：** 面向 LCJ 运营团队的高密度品牌经营指挥台，强调清楚、快速、可执行；个性为**可靠、果断、精密**。

**品牌语气：** 标题和操作文案直接描述任务，不加入营销式套话。例如“飛書同期”和“ブランド登録”继续保持动作明确；筛选提示聚焦当前范围，如“選択期間: 全期間”。

**字标与图标：** 延续现有橙红渐变的建筑图标作为本页识别符号，不替换全站品牌标志。

**标志性色彩：** `red-600` 对应的行动红继续作为本页面最具识别度的主操作色。
