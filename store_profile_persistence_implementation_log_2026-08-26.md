# 店铺人工资料消失问题实施日志

**作者：Manus AI**

**日期：2026-08-26**

## 问题现象

用户在店铺管理填写负责人后，后续部署重启使多个店铺重新显示“负责人未指定”。生产健康快照显示5家店铺中仅1家有负责人，0家有头像，0家有联系方式。

## 直接根因

`server/gmvHrRecovery.ts`的一次性GMV/HR恢复在判定店铺数量或2026年7月GMV发生漂移后，会重跑`restoreStores()`。旧实现对已存在店铺执行破坏性`UPDATE`：把负责人名称重写为截图基线，把主/副负责人ID与副负责人名称设为`NULL`，并覆盖备注；同时删除并重建用户的7月`store_data_uploads`记录。恢复完整性使用可编辑店铺名称和精确GMV作为触发条件，因此改名、扩店或上传新数据都可能导致重跑。

## 可恢复边界

本地保存证据和生产快照只能直接确认`LCJチャンネル=王铸`。用户刚填写后消失的其他负责人、头像、联系方式、副负责人和人工备注没有进入可读取快照，不能猜测恢复。恒久修复后，这些字段只需补录一次。

## 恒久修复

GMV/HR恢复完成判定改为使用恢复标记中的稳定店铺ID；名称、GMV和新增店铺不再触发破坏性重跑。恢复已存在店铺时只补空基础字段，不清空负责人、头像、联系方式、备注或用户上传数据。店铺创建、编辑和软删除改为事务，并新增`store_profile_audit_logs`保存变化字段、前后快照、操作人、来源和时间。服务端依据HR人员ID重新读取姓名，确保负责人ID与名称一致；自定义负责人保存名称并将ID置空；显式清空仍被允许且写入审计。

## 迁移与验证

`store-profile-v2-protect`已在服务监听前执行并新增`store_profile_audit_logs`。Railway前置加密备份ID 69成功（341表、10,841行），后置加密备份ID 70成功（342表、10,842行）。生产健康为`healthy=true`，`manualProfileProtection=true`，缺失表为0，5家店铺完整；当前直接可确认1家负责人、0家头像、0家联系方式，审计表初始为0行，未擅自补写无法证明的值。

确定性检查20/20通过；GMV恢复、改名/扩店、负责人语义、事务审计、v2迁移、商品迁移与头像上传六个风险域复核6/6通过。生产路由与StoreManagement分块均为HTTP 200，防覆盖说明、资料变更记录、保护状态和`profileAudit`标记均已上线；随机失效资产保持404且`Cache-Control: no-store`。Git功能提交为`fbf65025`。
