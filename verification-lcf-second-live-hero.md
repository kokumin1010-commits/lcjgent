# 第2回LCF ファーストビュー実Web UI化 検証記録

## ローカル表示確認

2026年9月17日、Vite単体プレビューの`/2nd`をデスクトップ幅で確認した。ヘッダーには既存の黄色いLCFロゴだけを表示し、指定キービジュアル内の左上ロゴはクロップ領域から外した。企業・ブランド申込とライブコマーサー申込は画像ではなく実リンクとしてヒーロー上部へ配置され、URLはそれぞれ第2回専用申込を維持している。

ファーストビュー左側のコピー、イベント名、日程、会場はDOMテキストとして表示し、右側は指定画像から出演者を中心に拡大クロップした。画像内に埋め込まれた疑似申込ボタンとイベントロゴは表示されず、右端の元画像コピーは色面で覆い、サイト側のキャッチコピーだけが表示される構成にした。

公式映像は`youtube-nocookie.com`の埋め込みだけを残し、「YouTubeで見る」外部リンクと`youtube.com/watch`への導線を削除した。ページ内プレイヤーのミュート自動再生、手動操作、音声切替、全画面は維持している。

デスクトップ確認では、実申込ボタン2本を白い上部操作帯へ配置し、その下を左のDOM情報面と右の人物ビジュアルへ分割した。元画像の疑似ボタンと左上ロゴは表示されず、ヘッダーの黄色LCFロゴだけが最上部に残る。人物写真に含まれる元コピーとの重複はクロップ位置とグラデーションで除去した。

モバイル確認では、黄色LCFロゴ、実申込ボタン2本、人物ビジュアル、DOMイベント情報の順へ変更した。390×844pxでボタンの横切れや横スクロールはなく、人物3名が最初の画面内に表示された。元画像に含まれる文字断片を避けるため、モバイルだけ背景の縦位置を下へ移し、右端は全高グラデーションで処理した。

最終目視では、1440×1000pxで左側にHTMLのコピー・イベント名・日程・会場、右側に人物3名を配置し、上部の申込ボタン2本を実リンクとして表示した。元画像の右端に残る埋込み文字はブランドカラーの不透明面から自然にフェードする処理で覆い、白いDOMキャッチコピーだけを表示した。

390×844pxでは、黄色ロゴ1つ、申込ボタン2本、人物3名、DOMキャッチコピーの順で表示される。画像内の疑似ロゴ・疑似申込ボタンは見えず、ボタンは左右を切らず縦積みで操作できる。人物写真の読み込みはCDN待機後に完了し、レイアウトシフトを抑える固定高を設定している。

## 自動回帰とビルド

第2回ページ専用回帰は1ファイル15件が成功した。第2回申込、共通アカウント、QR・受付、第1回レポート、LCMを含む関連回帰は43ファイル300件が成功した。テスト時のMySQL接続拒否は既存のローカルDB非起動環境による初期化警告であり、対象のソース契約テストは全件成功している。

`pnpm run check`全体は既存負債によりexit 2だが、今回変更した`LcfSecondEdition.tsx`と`lcf-second-edition-page.test.ts`の新規診断は0件だった。`pnpm run build`はexit 0で完了し、クライアント・サーバー成果物を生成した。ビルド末尾のDB接続拒否は既存のマイグレーション継続設計によるもので、ビルド自体は成功している。

最終ローカル表示では、黄色ロゴはヘッダーの1つだけ、ヒーロー上部の申込ボタン2本は実リンク、人物背景に元画像の疑似ロゴ・疑似申込ボタン・埋込み文字は表示されないことを確認した。公式動画はページ内の`youtube-nocookie.com`埋め込みだけで、ページ独自の「YouTubeで見る」外部導線は存在しない。

## 本番確認

機能コミット`47cb4cc9`についてGitHub check成功とRailwayデプロイ成功を確認した。本番`https://www.livecommercefestival.com/2nd`では、ヘッダーの黄色LCFロゴ1つ、企業・ブランド申込とライブコマーサー申込の実リンク、左側のDOMイベント情報、右側の人物3名、DOMキャッチコピーが表示された。画像内の疑似ロゴ・疑似申込ボタン・文字断片は表示されていない。

本番を390×844pxでも撮影し、黄色ロゴ、縦積みの実申込ボタン2本、人物3名、DOMキャッチコピーが最初の画面内で横切れなく表示されることを確認した。本番の申込ボタンURLは第2回専用の`/lcf/apply/company?edition=2`と`/lcf/apply/liver?edition=2`である。動画セクションにはページ内プレイヤーだけがあり、ページ独自のYouTube外部視聴ボタンはない。

## 完成ビジュアル上部帯除外版

ユーザーの最終指定により、人物写真を分解してDOMで再構成する方式から、指定完成ビジュアルの上部ロゴ・疑似申込ボタン帯だけを除外し、残りを一体の画像として表示する方式へ変更した。サイトヘッダーの黄色LCFロゴは1つだけで、画像直前の企業・ブランド申込とライブコマーサー申込は実リンクである。

ローカルの1600×1000px表示では、実申込ボタン2本の直下に、主要コピー、イベントロゴ、人物3名、日程、会場、実績円までを含む完成ビジュアルが自然につながった。390×844pxでは実申込ボタンを縦積みにし、完成ビジュアルを横幅に合わせて全体縮小したため、人物、日程、実績円、下部の第1回実景写真まで横切れなく確認できた。画像内の上部ロゴ・疑似申込ボタン帯は表示されていない。

機能コミット`fde87bcb`はGitHub checkとRailwayでsuccessとなった。本番`/2nd`では、既存黄色LCFロゴ、サイト側の実申込リンク2本、上部帯を除外した完成ビジュアル、既存の黒背景説明、ページ内公式動画の順に表示された。ブラウザ抽出では申込リンクが個別のアンカー要素として認識され、完成ビジュアルにはコピー、イベントロゴ、人物3名、日程、会場、実績円、下部の第1回実景写真が含まれている。本番データへの書込み操作は行っていない。
