# LCFマイページ 第2回表示・申込運用・共通アカウント改善 設計

## 根本原因

| 症状 | 原因 | 修正方針 |
|---|---|---|
| 第2回申込済みなのに9月8日・9日、八芳園が表示される | `getMyApplication`、本人の日程変更API、マイページのイベント詳細が第1回`2026`へ固定されている | 単一の「現在申込」を廃止し、開催回・申込種別・申込IDを必ず持つ本人申込一覧を返す |
| 第2回カードと第1回申込詳細が同じ画面上で混ざる | 第2回は履歴API、第1回詳細は旧単一APIから別々に描画される | 開催回カード内に、その回の申込詳細、日程、QR、変更操作をまとめる |
| ブース予約が第1回固定 | 日付、枠、会場、ルールが2026年9月8日・9日に固定されている | 第1回履歴は読取専用で保存し、第2回は設定確定後に同じ予約方式を開催回別設定で開放する |
| 通常ログイン後にライブコマーサー画面へ飛ぶ | 単一ロールの既定遷移がLCM creator workspaceを返す | 明示returnがない通常ログインは統合LCF/LCMマイページへ戻す |
| パスワードメールが「LCF 2026」 | 初回イベント名が件名・HTMLへ固定されている | 「LCF / LCM 共通アカウント」へ統一する |

## マイページの情報階層

最上部は「LCF参加・QR」「LCMブランド・商品」「LCM配信者プロフィール」の三つに分ける。直下に、LCFは開催イベントの申込・QR・同行者・当日案内、LCMは日常の商品・ブランド・配信者プロフィール・商談を扱う常設マーケットであることを短く説明する。

開催情報は第2回を上、開催終了した第1回を下に置く。各開催回カードは、その回の正しい日付・会場、本人の申込種別・状態・参加日程、本人QR、同行者、ガイド、変更可能な操作だけを内包する。第1回は履歴として不変、第2回のみ開催前の変更操作を許可する。

## 第2回申込操作

| 操作 | 条件 | 監査・安全策 |
|---|---|---|
| 日程変更 | 本人所有の第2回ライブコマーサーまたは一般参加申込、未取消、未受付 | `eventYear + applicantType + applicationId + email`で所有権確認。変更前後を監査ログへ保存 |
| 申込取消 | 本人所有の第2回申込、未受付 | 理由必須、確認Dialog必須。申込は削除せず`cancelled`へ変更し、対象チケットを無効化 |
| 同行者追加 | 本人所有の有効な第2回申込 | 氏名・フリガナ・メールを別レコードで保存。本人とは別QRを発行。1申込10名まで |
| 同行者編集 | 有効な同行者、未受付 | 所有権確認、変更前後を監査ログへ保存 |
| 同行者取消 | 有効な同行者、未受付 | レコードとQRを削除せず無効化し、監査ログへ保存 |

企業出展は両日参加として扱い、本人の日程変更対象外とする。開催終了した第1回申込は変更・取消対象外とし、履歴、QR、受付人数、VIP、アフターパーティー、ブース履歴をそのまま保持する。

## 同行者とQR

同行者は本人申込の人数欄や同じQRの複数回利用ではなく、開催回・親申込・本人アカウントへ紐づく別レコードとする。チケットには開催回、本人または同行者、同行者ID、有効状態を加算的に保持する。取消後もチケット行と受付監査は残し、受付APIが無効チケットを拒否する。

## イベント詳細とガイド

第2回カードは、2026年12月8日・9日、東京都立産業貿易センター浜松町館2階展示室を共通定義から表示する。未確定の開場時刻、搬入、ブース枠等は推測せず、「確定後に掲載」と明記する。ガイドは、会場アクセス、本人・同行者QR、ブランド準備、ライブコマーサー準備、事前マッチング、証憑付きGMV報告、問い合わせを一画面で確認できる構成とする。

旧「参加準備チェックリスト」は削除する。事実に基づかない完了状態を表示せず、希望商材・マッチング・GMVは実装済みのマッチング運用、商品登録はLCMワークスペース、ブース予約は開催回別の予約欄へ分離する。

## ブース予約

第1回の2026年9月8日・9日、T13〜T24、チェックイン、取消・無効化履歴は変更しない。第2回は同じ予約・チェックイン方式を再利用するが、日付、ブース番号、時間枠、予約開始、1人上限、連続利用、設備、撤収時刻を別設定にする。設定未確定の間は予約ボタンを表示せず、「ルール確定後にマイページで受付開始」と案内する。

## 認証とメール

明示された安全なreturn URLは従来どおり尊重する。returnがない通常ログインは、ブランドのみ、ライブコマーサーのみ、複数役割のいずれでも`/lcf/mypage`を既定とし、本人が上部メニューから必要なワークスペースを選ぶ。

パスワード再設定・変更メールは、「LCF / LCM 共通アカウント」を件名・見出し・本文へ使用する。ログインURLは共通ログイン`/lcf/login`を維持し、イベント回名をアカウント名として使わない。
