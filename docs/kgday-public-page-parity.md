# KGDAY 公开页完整迁入 LCJ

参考页面：`https://kgdayreco-2kqllucq.manus.space/?code=bQvxHYfhGBtoxAeq4ybERC`

目标页面：`https://lcjmall.com/brand-day/kgday-2026`

## 页面结构

| 顺序 | 原页面区块 | LCJ 迁移要求 |
|---:|---|---|
| 1 | 双层顶部导航 | 保留品牌名、日期、霓虹报名按钮、KG标志、页面锚点、排行榜、出场者登录；所有链接改为当前 `slug` 下的 LCJ 原生路由 |
| 2 | Hero | 保留 `CREATOR CLASH`、三层主标题、副标题、3 DAYS CHALLENGE、活动日期、四格倒计时和报名 CTA |
| 3 | ABOUT THE CLASH | 保留活动说明和4张指标卡；内容根据已确认 KGDAY 规则显示 |
| 4 | PRIZE | 保留销售榜与配信时间榜奖金卡片 |
| 5 | MERIT | 保留赏金、实时排名、持续合作三项卡片 |
| 6 | ENTRY CONDITIONS | 保留4项参加条件 |
| 7 | JUDGING | 保留 AI 读取、本人确认、异常管理员审核和评分条；文案适配 LCJ 当前双轨审核规则 |
| 8 | HOW TO STREAM | 保留报名、TikTok LIVE、AI读取、自动反映/待审核四步 |
| 9 | FAQ | 保留3项折叠问答并更新为 LCJ 原生管理流程 |
| 10 | 最终 CTA | 保留报名截止、挑战文案和报名按钮 |
| 11 | 页脚 | 保留版权、排行榜、出场者登录；管理入口指向 `/master/brand-days` |

## 视觉与交互

完整复用原黑紫星轨背景、双重暗角、金色与粉紫渐变文字、玻璃面板、金色与霓虹粉按钮、品牌标志、字体层级、卡片圆角、发光阴影和星光呼吸动画。所有 CSS 必须限定在 `kgday-public-*` 或现有品牌日页面容器内，不能修改 LCJ 全局主题或其他商城页面。

桌面端保留原1000–1200px内容宽度和双层导航。820px以下隐藏锚点按钮，保留横向可滚动的排行榜与登录入口；390px下 Hero 标题、日期、倒计时、卡片、FAQ和 CTA 不得横向溢出。

## 第一轮视觉验收

桌面1440×1000首屏已与原站结构对齐：双层导航、两处KG标志、三层Hero标题、活动副标题、3 DAYS CHALLENGE、9月8日至9月10日三天、四格倒计时、霓虹报名按钮和下一区块标题均完整显示。390×844下导航、日期、倒计时和CTA无横向溢出；首轮发现「KYOGOKUクリエイター」末尾长音符被单独换行，需要保持整行并适度缩小手机字号。

第二轮390×844复核确认「KYOGOKUクリエイター」已完整保持一行，页面宽度严格为390px。桌面审查基准双栏、评分条和双轨审核文案清晰；右侧标题「有効データは確認後すぐ反映」最后一个字出现孤行，改回原站较短的「確認後すぐに自動確定」后再验收。

全部7个内容锚点、KG标志和 LCJ 原生报名/排行榜/创作者登录链接均经 Chromium 断言通过；页面运行时错误和失败请求均为0。品牌日基础测试6项通过，5个数据库专项文件在常规门禁中按环境变量设计跳过；LCJ Vite 生产构建成功。结束边界为9月11日00:00时的空 DAY 4 已由后端按排他终点修复，前端同时防御性过滤，Hero 只显示9月8日至9月10日【3日間】。

长页底部390px复核通过：FAQ 四项均完整换行，原生 `details` 可展开并显示回答；最终CTA完整显示报名截止、礼物图标、挑战文案、说明和报名按钮。页面 `scrollWidth` 与 `clientWidth` 均为390px，无横向溢出。审查基准右侧标题已恢复原站「確認後すぐに自動確定」，不再出现孤字换行。

发布前回归通过：品牌日基础测试6项、菜单/认证错误边界/店铺管理相关回归89项全部通过；Vite前端生产构建与esbuild服务端生产打包成功。仓库全量 TypeScript 仍有既有错误基线，但本次修改的 `BrandDayPortal`、品牌日CSS、`brandDayPublicRouter` 和菜单测试均为零错误，`git diff --check` 通过。

## 正式站视觉验收

提交 `aea3f9e9` 已部署至 `https://lcjmall.com/brand-day/kgday-2026`。生产 Chromium 桌面1440×1000与手机390×844均加载完整长页：双层导航、两处KG标志、三层Hero标题、9月8日至9月10日【3日間】、四格倒计时、霓虹报名按钮和下一内容区块与原站结构一致。手机标题完整一行，页面宽度严格为390px；全部7个区块、FAQ展开、最终CTA及 LCJ 原生报名/排行榜/创作者登录链接均通过自动断言。

正式只读复核通过：公开主页、报名、创作者登录、排行榜和 `/master/brand-days` 五个路由均为 HTTP 200；公开活动 API 返回 DAY 1–DAY 3 共3天。迁移数据未受页面重构影响，销售榜4行、配信榜4行、KG GMV合计35,201、有效配信合计2,684分钟，与迁移完成时的生产对账一致。

## LCJ 数据与路由

活动名称、挑战名、副标题、时区、开始/结束时间和 DAY 列表来自 `trpc.brandDay.publicPortal.event`。倒计时使用活动开始时间。报名、排行榜和创作者登录分别使用 `/brand-day/:slug/entry`、`/brand-day/:slug/ranking`、`/brand-day/:slug/creator/login`。公开页不得调用独立 Manus 站，也不得改动 LCJ 已迁移的数据、会话或审核接口。
