# 店铺资料编辑与月份显示规则（2026-08-26）

## 资料编辑

现有5个活动店铺、2026年7月GMV和所有上传历史保持不变。店铺资料编辑只更新`managed_stores`中的名称、平台、地区、店铺链接、主负责人、副负责人、联系邮箱、联系电话、备注与头像引用，不修改`store_data_uploads`。

主负责人和副负责人优先关联HR当前活动且未归档人员；同时允许输入自定义负责人名称。选择HR人员时同时保存ID和当时姓名，保证人员后来改名、退职或归档后仍能追溯。清空负责人时ID与姓名必须一起设为`NULL`。

## 头像

图片文件保存到S3/R2对象存储，MySQL只保存`avatarUrl`与`avatarKey`，禁止把Base64或二进制写入`managed_stores`。上传只允许JPEG、PNG、WebP，最大5MB，需要有效登录会话；文件键使用`store-avatars/{storeId-or-new}/{random}.{ext}`避免覆盖。

替换头像只更新数据库引用。清除头像将URL与key设为`NULL`，历史对象变为不可达但不执行不可逆底层删除。页面上传时必须显示本地/远端预览、上传中状态和失败原因。

## 月份规则

总览、店铺卡、排名、图表和进入店铺详情时必须严格使用用户选中的年月。2026年8月没有上传数据时，5个店铺GMV、订单、顾客、退款和各渠道全部为0，并显示“当月未上传，按0显示”；不得查询或展示2026年7月金额。

2026年7月仍显示保存的5店铺实绩，总GMV固定为¥134,334,533。月份切换只影响读取范围，不复制、不迁移、不覆盖任何上传行。

## 安全与验证

资料升级前后必须各完成一次Railway MySQL加密备份。编辑API使用受保护tRPC，上传REST端点必须认证、限制MIME与大小并记录结构化错误。生产验证至少覆盖5店铺数量、7月总额、8月0值、头像/负责人字段、页面分块、上传端点拒绝未认证请求和失效静态资源404/no-store。
