# LCF管理ダッシュボード・属性別一斉メール 検証記録

## 対象

LCF管理画面 `https://www.livecommercefestival.com/lcf/admin` のダッシュボード、申込管理、画面内メール機能を対象とした。ユーザー指示は、ダッシュボードの件数カードをタップ可能にすること、企業・ブランド、ライブコマーサー、一般参加、スポンサーなどの属性ごとに一斉メールを送れること、配信後の返信を従来どおりLCF管理画面内の個別スレッドで確認・返信できることだった。

## 実装内容

### ダッシュボード導線

ダッシュボードの各件数カードをボタン化した。企業申込、ライブコマーサー申込、一般参加は申込管理へ移動し、選択した申込種別と「すべての開催回」をURLで引き継ぐ。LINE登録はLINE登録一覧、スポンサーはスポンサー管理、総申込数は全申込一覧へ移動する。キーボード操作とフォーカス表示も維持している。

### 属性別LCF一斉メール

ダッシュボードと申込管理の両方に「一斉送信」を追加した。対象属性は企業・ブランド、ライブコマーサー、一般参加、スポンサーを複数選択できる。さらに開催回、参加日程、申込状態を絞り込める。申込管理から開いた場合は、現在表示している申込属性と開催回を初期値として引き継ぐ。

件名と本文には `{{name}}`、`{{company}}`、`{{event}}`、`{{type}}` の差し込みを利用できる。送信前に対象件数と実際の差し込み済みサンプルを表示し、メールアドレスの正規化、無効アドレス除外、同一メールアドレスの重複除外を行う。宛先はBCC一括ではなく、各受信者へ1通ずつ個別送信する。

### 誤送信・重複送信対策

一斉送信開始前には、対象件数を含む専用確認チェックを必須とした。確認前は開始ボタンが無効である。キャンペーン本体と受信者スナップショットはトランザクションで永続化し、受信者ごとの状態を `pending`、`processing`、`sent`、`failed`、`cancelled` で管理する。対象が多い場合は分割保存し、1通ずつ間隔を空けて処理する。SMTP受付後は送信済みに確定し、履歴保存の後段障害を理由に同じ宛先へ自動再送しない。停止操作、進捗表示、失敗件数表示、サーバー再起動後の未完了キャンペーン復旧も実装した。

### 返信のやり取り

一斉送信も既存のLCF個別メールと同じ送信元、Reply-To、HTMLデザイン、品質ガード、送信履歴基盤を使用する。各送信ログに申込種別・申込ID・開催回を保存するため、相手から届いた返信は従来のIMAP同期で受信履歴へ取り込まれ、該当申込の「LCFメール」ポップアップ内に表示される。受信メールの「このメールに返信」から件名と引用文を引き継ぎ、同じ画面で返信を作成できる。画面表示中は返信同期が継続し、全体メール履歴にも受信として反映される。

### メール表示

メールHTMLはLIVE COMMERCE FESTIVALの公式配信であること、送信元、返信可能であることが分かるブランドヘッダーと署名へ統一した。件名・本文の迷惑メール対策とLCF署名自動付与は既存の安全基準を維持している。

## データベース

`festival_bulk_email_campaigns` と `festival_bulk_email_recipients` を追加した。SQLマイグレーションは `drizzle/0141_lcf_bulk_email_campaigns.sql` として登録し、既存マイグレーション詰まりがあっても必要テーブルを冪等に補完するfallbackを `run-migrations.mjs` に追加した。Railway/TiDB互換性を優先し、親子整合はアプリ側のトランザクション、キャンペーンID、受信者単位の一意制約で維持する。

## 検証結果

| 検証 | 結果 |
|---|---|
| 専用回帰 | 3ファイル・22件成功 |
| LCF・LCM関連全回帰 | 40ファイル・278件成功 |
| Festival・メール関連回帰 | 5ファイル・47件成功 |
| 表示幅補正回帰 | 2ファイル・15件成功 |
| Production build | 成功。Vite・Express成果物生成済み |
| Migrationのローカル実行 | ローカルDB未起動のため既存どおり `ECONNREFUSED` 後に `Continuing despite error...` で継続 |
| TypeScript全体 | 既存783件の型負債。今回追加・変更した一斉送信関連ファイルの新規診断なし |
| GitHub Check | `cf0790c3` と `6b316714` が success |
| Railway | deployment `6540488516` と `6540545201` が success |

## 本番受入

本番ダッシュボードでは、企業申込40件、ライブコマーサー申込466件、一般参加337件、LINE登録0件、スポンサー0件、総申込数843件の各カードがボタンとして表示された。ライブコマーサーカードのタップで `/lcf/admin?tab=applications&type=liver&event=all` へ移動し、ライブコマーサー属性が引き継がれた。

一斉送信ポップアップではライブコマーサーだけが初期選択され、「すべての開催回」「参加確定」でプレビューした結果、重複・無効アドレス除外後の対象は465件だった。最終確認チェックは未選択、開始ボタンは無効、キャンペーンは未作成であり、検証中に本番メールは1通も送信していない。

幅補正後の本番PC 1280pxでは、ポップアップ幅は1120px、内部の `clientWidth` と `scrollWidth` はともに1118px、ページ横溢れは0pxだった。対象選択、本文、差し込み済みサンプル、確認欄を横スクロールなしで確認できる。

個別LCFメールも本番で開き、画面内送信ボタンと「届いた返信を画面表示中に自動反映します」の表示を確認した。確認対象には受信メールがなかったため、その対象で返信ボタンが出ないことは正常である。返信機能自体は受信カードから「このメールに返信」を表示する回帰契約で保全している。

## 安全上の結論

機能は本番反映済みだが、一斉送信は自動開始しない。対象プレビュー後に件数を明記した確認チェックを管理者が選択し、開始ボタンを押した場合のみ配信キューを作成する。今回の受入では対象確認までに留め、実送信、申込データ変更、会員データ変更は行っていない。
