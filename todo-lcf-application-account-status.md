# LCF申込管理・アカウント状態表示

- [x] 最新`main`と`WORK_LOG.md`を確認し、申込管理・アカウント管理の現行API、テーブル、メール正規化ルールを監査する。
- [x] 企業・ライバー・一般参加の各申込に対し、同一メールのログインアカウント有無を安全に返すサーバー契約を設計する。
- [x] 申込管理一覧へ「アカウントあり／未作成」を追加し、検索・CSV・詳細・受付状態など既存操作を壊さない。
- [x] 同一メールの複数申込、大小文字、前後空白、空メール、管理者アカウントを含む境界条件を回帰テストする。
- [x] LCF・申込・アカウント・受付・予約関連26ファイル164項と完全生产构建を確認する。
- [x] 認証済み本番管理画面でアカウント状態バッジと既存一覧レイアウトを確認する。
- [x] 初回実装を`WORK_LOG.md`へ記録し、remote `main`同期後にcommit・pushする。
- [x] 初回実装のGitHub CI、Railway、本番管理画面を確認し、業務データを変更していないことを確認する。

## バッジからアカウント管理への直接導線

- [x] 「アカウントあり／アカウント停止中」を操作可能にし、アカウント管理タブへ移動する。
- [x] 移動後は該当メールで一覧を絞り込み、対象行を強調し、全件表示へ戻せるようにする。
- [x] 「未作成」は遷移先がないため非操作表示を維持する。
- [x] キーボード操作、既存タブ切替、CSV、パスワード再設定を回帰確認する。
- [x] 追加導線をcommit・pushし、GitHub CI・Railway・本番配布資産・管理者認可を確認する。
