# HR 離職者・安全削除（アーカイブ）ルール

`/master/hr`での「削除」は、人物行を物理削除せず、**離職確認済み人物を人物目录から非表示にするソフト削除（アーカイブ）**として実装する。

| 操作 | 人物目录 | 日報・タスク・給与・評価 | ログイン | 元に戻す |
| --- | --- | --- | --- | --- |
| 退職処理 | 退職確認済みとして表示 | 保持 | 既存処理で無効化 | 復職可能 |
| 目录から削除（アーカイブ） | デフォルト目录から非表示 | **すべて保持** | 退職時の無効状態を維持 | アーカイブ箱から復元可能 |
| 復元 | 退職確認済みとして再表示 | 保持 | 自動再有効化しない | その後、必要なら別途復職 |
| 完全削除 | HR画面では提供しない | 参照破壊の危険あり | 対象外 | 不可逆のため別途明示確認が必要 |

既存の`staff.delete`はDB行を物理削除する実装で、`tasks.staffId`、`task_staff.staffId`、`report_staff.linkedStaffId`および給与・評価の履歴参照を壊す可能性がある。新しいHR画面と旧スタッフ管理画面の一般削除操作は、物理削除ではなくアーカイブAPIへ統一する。

アーカイブ対象は原則として`resignDate`が存在する離職確認済み人物に限定する。未リンクの`report_staff`だけの人物は、離職日を直接証明できないため通常削除対象にせず、まずスタッフ情報と離職日を確認する。

アーカイブは`staff.archivedAt`、`staff.archivedBy`、`staff.archiveReason`と監査イベント表に記録する。`report_staff`行やリンクは削除しない。復元はアーカイブ列だけをクリアし、`isActive='inactive'`、`resignDate`、`resignReason`は維持するため、誤って在籍中へ戻らない。

既存HR復元モジュールは人物行を再利用してもアーカイブ列を上書きしないため、起動時にアーカイブ済み離職者が自動再表示されない設計とする。
