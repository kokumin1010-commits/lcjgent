# LCF签到与直播预约端到端测试

- [x] 同步远端`main`并读取最新`WORK_LOG.md`，确认无并发改动。
- [x] 梳理入场二维码累计签到、直播预约签到、现场预约直播的页面、API、权限和撤销路径。
- [x] 运行三条流程相关的自动化回归与完整生产构建。
- [x] 在生产环境只读检查管理员会话、测试账号资格、可用时段与基线聚合数据。
- [x] 在获得用户确认后，对专用测试票券执行一次入场`＋1名`并立即撤销，累计人数恢复原值。
- [x] 使用生产有效ブースQR验证开放前直播预约签到被服务器拒绝，临时预约已取消且无`checked_in`残留。
- [x] 在获得用户确认后，执行一次事前直播预约创建→本人取消测试，并恢复账号与时段基线。
- [ ] 在9月8日／9日对应时段开始前15分钟内，执行一次真实`same_day`现场预约并立即取消；当前9月7日服务器仅允许`advance`。
- [x] 核对生产统计、审计和既有预约未受影响；入场、资格和有效预约均恢复基线。
- [x] 在`WORK_LOG.md`末尾追加脱敏验收记录；未发现代码缺陷，不创建功能代码提交。

## 测试边界

入场签到由管理员API执行，带请求幂等ID，并可撤销该票券最近一次有效入场事件。直播预约签到与现场预约均要求登录中的有效ライバー账号和有效ブースQR；预约签到只在该时段开始前15分钟至时段结束前开放。现场预约在活动日前属于事前预约，只有活动当天且进入该15分钟窗口后才会被服务器判定为`same_day`，因此9月7日不能伪造一次真实生产“现场预约”，只能通过时间固定的自动化回归验证该分支，并在9月8日／9日窗口内进行真实现场测试。

## 自动化验证

累计入场、管理员受付校验、VIP与アフターパーティー共存、ブース预约策略、ブースQR、预约规则、多身份权限与CSV相关9个测试文件共68项全部通过；完整生产构建成功。测试期间本地未配置数据库，既有初始化器输出连接失败日志但测试通过；构建中的既有Sharp namespace warning与本任务无关。

## 生产验证

专用ライバー测试账号的入场票券从1名临时增加至2名，随后撤销回1名；全站累计来场保持22、受付済みQR保持12。创建并取消2条T16测试预约后，账号活动预约为0、ブース有效预约保持54，历史仅增加2条已取消审计记录。生产有效T16 QR在9月7日返回无可签到预约，签到未成功写入；服务器对活动时段仍返回`advance`模式。真正`same_day`现场预约及成功签到只能在9月8日／9日相应时段开始前15分钟内验证，不能通过修改生产时间绕过门禁。
