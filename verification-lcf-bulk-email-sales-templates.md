# LCF属性別一斉メール：営業テンプレート保存 検証記録

## 対象

LCF管理画面 `https://www.livecommercefestival.com/lcf/admin?tab=applications` の「属性別 LCF一斉メール」に、営業文面を保存・再利用できるテンプレート機能を追加した。

## 実装内容

テンプレートは**件名と本文だけ**を保存し、宛先属性、開催回、参加日程、申込状態は配信のたびに選び直す設計とした。これにより、過去の抽出条件を誤って再利用する事故を防ぎながら、営業文面だけを安全に再利用できる。

分類は「営業」「開催案内」「フォロー」「その他」の4種類とした。テンプレート名、分類、件名、本文を保存でき、保存済みテンプレートの呼び出し、上書き保存、削除が可能である。同一分類・同一名の重複保存は拒否し、保存時には個別LCFメールと同じ件名・本文品質ガードを適用する。

テンプレート管理APIはLCF管理者認証下に限定した。保存・更新・削除はLCF操作履歴へ記録するが、本文全体は監査ログへ保存しない。永続化には `festival_bulk_email_templates` を追加し、Drizzle migration、既存migration詰まり用fallback、API実行時の `CREATE TABLE IF NOT EXISTS` の三段階で安全に作成する。API実行時の自己修復は同一のin-flight Promiseを共有し、失敗時だけ再試行可能な状態へ戻す。

## 本番へ保存した営業テンプレート

| 項目 | 内容 |
|---|---|
| テンプレート名 | `LCF営業案内` |
| 分類 | 営業 |
| 件名 | `【LIVE COMMERCE FESTIVAL】開催に関するご案内` |
| 宛名差し込み | `{{name}} 様` |
| 本文要旨 | LCF運営事務局から、企業・ブランド、ライブコマーサー、来場者が商談・配信・販売へつながる公式イベントであること、開催・参加の最新情報、メール返信による問い合わせ方法を案内 |

## 検証結果

| 検証 | 結果 |
|---|---|
| 専用回帰 | 3 files / 24 tests 成功 |
| 自己修復追加後の専用回帰 | 2 files / 16 tests 成功 |
| LCF・LCM関連全回帰 | 40 files / 280 tests 成功 |
| Festival・メール基盤回帰 | 5 files / 41 tests 成功 |
| Production build | 成功。ローカルDB未起動によるmigration `ECONNREFUSED` は既存スクリプトが継続し、Vite・Express成果物を生成 |
| TypeScript全体 | 既存748診断。今回変更したテンプレートサービス・回帰に新規診断なし |
| GitHub Check | `04f258aed74022d4cdcc6294f2d8a3ea3ee9e7a8` success |
| Railway | deployment `6542029328`、`lcjagent / production` success |
| 本番UI | ダイアログ幅1120px、ページ横溢れ0px、テンプレート名・分類・保存済み一覧・新規保存・上書き保存・削除を確認 |
| 永続保存 | 新規保存後に `1件保存` と `[営業] LCF営業案内` を確認。ページ再読込後も同じ1件を取得 |
| 呼び出し | テンプレート名・件名・本文が完全復元されることを確認 |
| 上書き | `「LCF営業案内」を上書き保存しました` を確認 |

初回本番保存ではdeployment migration経由でテーブルが作られていない状態を検出した。この時点では保存処理が安全に失敗し、メール送信やキャンペーン作成は行われなかった。API自身の冪等スキーマ自己修復を追加して再デプロイし、その後の新規保存、ページ再読込、呼び出し、上書きを本番で成功確認した。

本番検証では営業テンプレート1件の保存と上書きだけを実行した。**一斉送信の対象確認、キャンペーン作成、実メール送信、申込・会員データ変更は実行していない。**
