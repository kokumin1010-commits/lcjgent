# LCF・LCM共通ログイン／役割別マイページ設計

## 基本方針

LCFとLCMは既存の`festival_accounts`、HttpOnly cookie、`/lcf/login`を唯一の認証基盤として使用する。LCM専用アカウント・専用パスワード・別ログイン画面は作らない。ログイン画面では「LCF / LCM 共通アカウント」であることを明示し、認証後は安全な同一サイト内returnがある場合にそれを優先し、ない場合は共通マイページ`/lcf/mypage`へ進む。

## 役割判定

役割は`festival_accounts.accountType`だけで固定せず、同じメールアドレスに紐づく有効な企業申込・ライバー申込と、既存LCM会員・ブランド権限・公開プロフィールを統合して判定する。拒否・取消済み申込は資格に含めない。既存アカウント種別を書き換えず、データ移行や新規環境変数も追加しない。

| 役割 | 判定元 | 主画面 |
|---|---|---|
| イベント参加者 | 有効なLCFアカウント | `/lcf/mypage` |
| ブランド・企業 | 有効な企業申込、会社・事務所LCM会員、既存ブランド権限のいずれか | `/lcm/manage?workspace=brand` |
| ライバー | 有効なライバー申込、ライバーLCM会員、既存クリエイタープロフィールのいずれか | `/lcm/manage?workspace=creator` |
| 複数ロール | 上記を複数満たす | 共通ナビで常時切替 |

## 画面設計

LCFマイページ上部に「マイページ切替」を追加し、利用可能な役割だけを表示する。イベント履歴・QR・予約はイベントマイページに残す。ブランド管理とライバー公式ページは既存LCMワークスペースを再利用し、両資格を持つ利用者はクエリパラメータで明示的に切り替える。資格のない画面は表示せず、直接URLでもサーバー権限検査を維持する。

## セキュリティ

returnは同一オリジンの相対パスだけを許可し、`//`、バックスラッシュ、制御文字、外部URLを拒否する。クライアント側だけでなくテスト可能な純粋関数へ分離する。LCMのブランド作成は、LCM会員種別だけでなく有効な企業申込または既存ブランド権限を確認する。ライバープロフィールは既存の有効ライバー申込・本人公開同意・運営審査を引き続き必須とする。

## 既存機能保護

同一メールの企業・一般・ライバー複数申込、入場チケット、ブース予約、LCM会員審査、ブランド所有権審査、ライバー公開同意、管理者ログインを変更しない。一般参加者はLCM会員申請を行わない限り、イベントマイページのみを使用する。
