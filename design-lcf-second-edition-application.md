# 第2回LCF申込・開催回分離設計

## 目的

2026年9月8日・9日の第1回データを一切上書きせず、2026年12月8日・9日の第2回申込を、既存のLCFログインID・パスワード・会員マスタで受け付ける。

## 開催回キー

| 開催回 | `event_year`保存値 | 表示名 | 日程 | 会場 |
|---|---|---|---|---|
| 第1回 | `2026` | 第1回 LIVE COMMERCE FESTIVAL 2026 | 2026年9月8日・9日 | 八芳園 |
| 第2回 | `2026-02` | 第2回 LIVE COMMERCE FESTIVAL | 2026年12月8日・9日 | 東京都立産業貿易センター浜松町館 2階展示室 |

既存列は`varchar(10)`であり、`2026-02`を保持できる。第1回既存行の更新・移行・再採番は行わない。

## 申込URLとAPI

- 企業・ブランド申込: `/lcf/apply/company?edition=2`
- ライブコマーサー申込: `/lcf/apply/liver?edition=2`
- クエリ値はクライアント・サーバー双方で許可リスト検証し、第2回は`2026-02`として送信・保存する。
- クエリなしの既存フォーム/API呼出しは後方互換のため`2026`として扱う。
- 第2回ページ・TOPの新規CTAだけが`edition=2`を指定する。

## 会員とパスワード

- `festival_accounts`は開催回をまたいで共通利用する。
- 同じメールの既存アカウントがある場合、アカウントを新規作成せず、パスワード・主アカウント種別・既存applicationIdを変更しない。
- 申込の資格はメールと開催回別申込行から判定し、企業・ライブコマーサーの複数資格を同一アカウントで保持する。

## メールアドレス起点の第2回申込UX

第2回の企業・ブランド申込とライブコマーサー申込は、最初の質問をメールアドレスにする。第1回フォームは既存の質問順を維持し、`edition=2`のときだけ順番を変更する。

| 入力後の状態 | 画面表示 | 処理 |
|---|---|---|
| 既存LCF会員 | 「会員様、ありがとうございます。第1回と同じアカウントで、第2回のお申し込みを続けられます。」に続けて次の質問を表示する | 新しいアカウントやパスワードを作らず、第2回の必要項目へ進む |
| 新規メール | 「メールアドレスありがとうございます。第2回のお申し込みを続けます。」に続けて次の質問を表示する | 既存フォームを継続し、申込完了時だけ従来方式でアカウントを作成する |
| 照合通信エラー | 新規・既存を断定せず「メールアドレスありがとうございます。第2回のお申し込みを続けます。」と表示する | 申込を中断させず、最終送信時の既存会員再利用ロジックを正とする |

会員確認APIは、正規化したメールアドレスと許可済み`edition`だけを入力に取り、返却値は有効な既存会員かどうかの真偽値だけに限定する。氏名、アカウント種別、applicationId、ログイン履歴、申込履歴、パスワード状態は返さない。IP単位およびIP＋メール単位で回数制限を設け、連続照会による会員リスト探索を抑制する。画面で既存会員を確認できても認証済みログインとは扱わず、マイページや過去履歴は既存のID・パスワードでログインした後だけ表示する。

第2回ライブコマーサーフォームの保存キーは、質問順変更前の途中保存データを誤った質問へ復元しないよう、メール先頭版としてバージョンを分ける。第1回フォームの保存キーと質問順は変更しない。

## 既存会員の本人確認と重複入力省略

第2回フォームでメール照合結果が既存会員だった場合だけ、次のステップとして既存パスワードを求める。認証には共通LCFログインと同じPBKDF2検証、回数制限、HttpOnly Cookieを利用する。パスワードは申込回答、チャット履歴、localStorage、サーバーログへ保存せず、画面上でも伏字として扱う。

| 状態 | 次の画面 | 再利用する情報 |
|---|---|---|
| 既存メール・パスワード一致・同種別の第1回申込あり | 「会員様、ありがとうございます」と表示し簡略申込へ進む | 本人確認済みの第1回申込から共通プロフィールをサーバーで取得 |
| 既存メール・パスワード一致・同種別の第1回申込なし | 本人確認済みであることを表示し、同種別の必要項目を入力 | メールと共通アカウントだけを利用 |
| 既存メール・パスワード不一致 | その場に留まり、共通のエラーとパスワード再設定導線を表示 | 登録情報は返さない |
| 新規メール | パスワードを求めず従来の全項目入力へ進む | 申込完了時だけ新規アカウント作成 |

企業申込では会社名、担当者、部署、フリガナ、住所、電話、Webサイト、連絡先、TikTok Shop情報、ブランド紹介、ターゲット、販売資格を引き継ぎ、第2回のマッチング商材と同意だけを再確認する。ライブコマーサー申込では氏名、フリガナ、活動名、事務所、SNS、ジャンル、電話、連絡先を引き継ぎ、第2回の来場希望日、事前マッチング希望、同意だけを再確認する。引継ぎ元は同じメール・同じ申込種別の第1回`event_year='2026'`最新行とし、第1回行を更新しない。

## マイページ入口の統合

公開ヘッダーでは「マイLCM」と「共通マイページ」を並べず、ログイン済み・未ログインとも入口を「マイページ」1つにする。未ログイン時は共通ログインへ送り、認証後は`/lcf/mypage`へ戻す。統合マイページ内では既存の役割ナビを利用し、イベント、ブランド、ライブコマーサーを切り替える。既存の`/lcm/manage?workspace=brand|creator`は機能URLとして保持し、ブックマークや深いリンクを壊さない。

## 共通ログインと役割別自動案内

公開ページの未認証入口は「ログイン」、認証済み入口は「マイページ」と表示する。ブランド、ライブコマーサー、イベント参加者でログインフォームを分けず、メールアドレスとパスワードによる`/lcf/login`を唯一の入口とする。公開文言は「ライバー」を使わず「ライブコマーサー」へ統一するが、DB・API・URLの既存`liver`／`creator`識別子は後方互換のため変更しない。

ログイン成功後は、同一メールに紐づく有効な企業申込、ライブコマーサー申込、LCM会員・ブランド管理権限、ライブコマーサープロフィールをサーバーで判定する。明示的な安全な同一サイト`return`がある場合はそれを最優先し、通常ログインでは次の順で案内する。

| 認証後の役割 | 遷移先 |
|---|---|
| 管理者 | `/lcf/admin` |
| ブランドのみ | `/lcm/manage?workspace=brand` |
| ライブコマーサーのみ | `/lcm/manage?workspace=creator` |
| イベント参加のみ・役割未設定 | `/lcf/mypage` |
| ブランドとライブコマーサー等の複数役割 | `/lcf/mypage`で役割を選択 |

保護されたLCM URLを未認証で開いた場合は、「ログインが必要です」だけの中間画面を表示せず、同じURLを安全な`return`として共通ログインへ直接移動する。

## 申込不要の共通アカウント新規登録

共通ログインページ内で「ログイン」と「新規登録」を切り替えられるようにする。新規登録はイベント申込を前提とせず、メールアドレス、表示名、本人が設定するパスワード、利用目的、利用条件への同意だけで`festival_accounts`へ共通アカウントを作成する。`application_id`は`null`とし、イベント申込、QR、予約、来場履歴、申込メールは作成しない。

| 利用目的 | 保存する既存`account_type` | 登録後の案内 |
|---|---|---|
| 企業・ブランド | `company` | `/lcm/manage?workspace=brand`でLCM初回プロフィール設定 |
| ライブコマーサー | `liver` | `/lcm/manage?workspace=creator`でLCM初回プロフィール設定 |
| イベント参加・情報閲覧 | `general` | `/lcf/mypage` |

登録APIは入力された任意のアカウント種別を受けず、許可済み利用目的からサーバー内で既存`account_type`へ変換する。メールは小文字・前後空白除去で正規化し、既存メールは新規作成せずログインへ案内する。パスワードは8〜128文字、英字と数字をそれぞれ1文字以上含め、既存PBKDF2方式でハッシュ化する。IP単位・IP＋メール単位の回数制限、HttpOnly・Secure・SameSite=Lax cookie、活動ログを適用する。登録成功時はそのまま認証済みにする。

申込不要の新規アカウントを後日第2回フォームで使用した場合、同じメールとパスワードで本人確認し、既存会員として第2回申込だけを追加する。第1回・第2回申込、QR、履歴と会員マスタは従来どおり分離する。

## 第2回ヒーローの構図

元画像は変更せず、ヒーローの最小高さと`object-position`を調整する。デスクトップでは天井空間より黄色いLCF看板と来場者動線を優先して下側を見せ、ファーストビュー内にコピーとCTAが入る高さへ詰める。モバイルでは見出しとCTAの可読性を守り、過度な縦長表示を避ける。

## 重複防止

- 重複判定は`email + event_year + applicantType`単位とする。
- 第1回に同じメールの申込があっても、第2回は新規申込として保存する。
- 第2回で同じ種別へ再送した場合は、既存第2回申込・既存第2回チケットを返し、重複行・重複QRを作らない。

## チケット・マイページ

- チケットID・QR仕様は変更しない。新規第2回申込行に対して新しいチケットを発行する。
- `getMyEditionHistory`は申込テーブルの`event_year`から開催回を解決する既存方式を継続する。
- `getMyTickets`は申込テーブルへ戻して`eventYear`を返し、マイページ上で第1回・第2回のQRを開催回別に表示できるようにする。
- 第1回QR、受付回数、VIP、アフターパーティー履歴は変更しない。

## メール

- 第2回企業受付メール、ライブコマーサー受付メール、QRメールは「2026年12月8日・9日」「浜松町館2階」を表示する。
- 第1回の再送・管理操作では従来の日程・会場を表示する。
- メール関数は`eventYear`を引数で受け、許可された開催回定義から表示文言を決定する。呼出側の自由入力は本文へ挿入しない。

## 管理画面

- 既存の`eventYear`フィルタを利用し、第2回は`2026-02`で絞り込む。
- 画面上の表示ラベルは保存値を直接見せず、「第1回」「第2回」と日程・会場へ変換する。

## 第2回の体験価値

第2回ページでは、確定した機能だけを掲載する。現時点では、企業とライブコマーサーの直接マッチング、商品体験、実践セミナー、会場からのライブ販売、イベント後のLCM継続商談を中心に説明する。リアルタイム売上表示や公式ライブスタジオ等は、運営仕様が確定するまで予定・構想としても断定表示しない。
