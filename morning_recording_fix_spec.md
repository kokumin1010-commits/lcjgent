# 早会录音完成判定与删除修复规格

**范围：** 仅`/master/morning-meeting`、`server/morningMeetingRouter.ts`、`server/teamMorningMeetingPolicy.ts`和早会专用验证文件。HR、报告员工、店铺、商品、主播、积分等模块保持不变。

## 已确认根因

生产设置`minimumDurationSeconds=60`于2026-08-27 07:08:53 UTC写入。当天14条个人9条朗读中，13条为3–52秒；这些录音此前已经正常保存为`completed`，但后续代码用60秒规则重新判定，导致上午显示完成的记录在下午部署后变成“时长不足”。用户已明确说明熟悉内容后可以在一分钟内读完，因此个人朗读和团队早会都不再设最低秒数。

当天另有1条280秒、12人参加的`daily_team`记录。它创建于中国/日本团队拆分前，`teamCode=legacy`、`dailyKey=2026-08-27:legacy`；参与者对应当前在职员工时为中国团队12/12，因此可依据保存的参与者快照无歧义地作为中国团队早会显示。当前页面只按`teamCode=china|japan`查找，导致这条真实录音未计入团队完成。

## 完成判定

1. 完全移除个人9条朗读和团队早会的最低时长设置、前端计时禁用、服务端秒数校验以及历史“时长不足”追溯判定。
2. 录音仍必须通过文件格式、大小、非空内容、登录权限、日期一致性和服务端处理流程；只有服务端状态为`completed`才计为完成。
3. 今天已保存的13条短个人朗读自动恢复为完成，不修改或复制数据库记录，也不要求重新录制。
4. 团队拆分前的`daily_team + teamCode=legacy`记录，只在参与者快照中的全部可识别在职员工均属于同一团队时，按该团队读取；混合团队或无法识别时不得猜测。
5. 无歧义归类的旧团队记录同时参与今日完成、参加人数和重复录制阻止逻辑，避免保存第二条重复团队早会。

## 删除权限与审计

| 记录类型 | 普通用户 | 管理员 |
|---|---|---|
| 个人9条朗读/本人早会 | 仅删除本人记录 | 可删除任意记录 |
| 中国/日本/旧团队早会 | 仅创建该记录的主持人 | 可删除任意记录 |

删除前必须二次确认。服务端在同一事务中验证权限，把日期、类型、目标/创建者、时长、状态和参与人数等非音频元数据写入`manual_data_change_events`，随后删除业务记录；审计失败则整笔回滚。审计中不保存音频URL、对象key、转写全文或AI摘要。

删除数据库引用后，对象存储key不再可通过页面/API访问；现有存储层不提供物理对象删除接口，因此不新增不安全的S3删除实现。删除成功后，今日状态、历史列表和完成进度均刷新；个人记录可重新录制，团队记录释放对应日期/团队。UI仅在服务端返回`canDelete=true`时显示删除按钮，服务端仍独立执行权限校验。

## 非破坏与验证

不自动删除生产中的14条个人朗读或280秒团队录音。功能部署只取消错误判定、正确显示旧中国团队记录并提供人工删除入口；生产验证只读，不点击删除。必须用mock数据库验证：任意长度`completed`录音有效、本人删除成功、他人删除403、管理员删除成功、审计失败回滚、刷新后消失、旧中国团队记录正确归类。提交差分不得包含任何非早会业务文件。
