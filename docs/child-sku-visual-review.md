# 父子SKU视觉回归检查

检查日期：2026-08-28

## 展开后的独立行

父商品下共显示3条子SKU独立行：2条商品内JSON SKU与1条实体子SKU。每条行均与父商品表格使用同一组列，清楚显示SKU名称、SKU编号、条码、品牌、继承类目、定价、历史最低价、保护期、佣金、库存、状态、促销组合与操作按钮。商品内SKU以`[商品SKU]`标识，实体子SKU以`[子SKU]`标识，层级缩进明确。`1+1`和`1+2`组合、公开中/下書き/非公開状态及库存数字均未发生列错位或遮挡。

## 连续三次修改后

同一商品内SKU从`10個セット`连续修改三次后，最终行显示`10個セット 第3回修改`、SKU编号`KG-SET-10-V3`、定价`¥5,100`、历史最低`30%OFF ¥3,400`、库存`28`、状态`公開中`和组合`1+4`。同级`20個セット`仍保持原名称、编号`KG-SET-20`、定价、库存、草稿状态及`1+2`组合，证明单行编辑未覆盖兄弟SKU。

实体子SKU最终显示更新后的名称、SKU编号`KG-KERATIN-MASK-5-NEW`、条码`4580000000999`、定价`¥4,300`、历史最低`¥3,500`、库存`28`、非公开状态及`1+2`组合。恢复来源ID和父级关系在测试状态中保持不变。

两张截图在1720×1100浏览器视口中检查，表格可横向滚动，行内容、状态徽标、编辑按钮和删除/解除按钮均清晰可操作，无重叠、截断导致的关键字段不可见或控制台错误。纯mock测试生产写入为0。

## 专用编辑弹窗与重新登录

子SKU专用弹窗在1720×1100视口中完整显示父商品与来源类型，并同时展示SKU名称、SKU编号、定价、历史最低价、折扣率、库存、状态和促销组合。取消与保存按钮位于弹窗底部且无遮挡；商品内SKU不显示不适用的实体条码字段。重新登录后的截图继续显示三条独立子SKU行和第3次修改后的最终值，证明UI状态并非只停留在当前浏览器会话。
