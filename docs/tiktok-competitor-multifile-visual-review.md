# TikTok竞品日报多文件视觉检查

检查日期：2026-08-28（纯mock本地Chromium；生产写入0）

## 多文件独立预览

`multifile_pending_previews.png`显示同一次选择的`kalodata-morning.csv`与`kalodata-evening.csv`分别形成独立卡片。两份文件均显示文件名、大小、识别行数、排除行数、前5店及各自的“保存为独立批次”按钮；两张店铺明细表无重叠或遮挡。页面明确提示“2份互不覆盖”。

## 两批次并排对比

`two_batches_comparison.png`显示两份文件分别保留为批次#101与#102，并同时勾选。批次卡片显示导入人、日报引用数、原文件与查看入口。店铺对比表按批次展示排名、销量、GMV，并显示首份到末份变化；商品对比表展示成交价、销量、GMV及变化值。第二批次标记为“最新”，第一批次仍完整保留。页面下方既有日报仍保持原店铺与15/15完成状态，证明对比区未覆盖日报内容。

## 结论

视觉布局清晰，上传、批次历史、单批次入口和对比结果处于同一页面，适合连续上传后立即比较。未发现文字截断导致的不可操作、表格重叠或关键按钮遮挡。

## 单批次独立查看

`first_batch_independent_view.png`显示第二份文件保存后，第一份`kalodata-morning.csv`仍可单独打开批次#101，完整展示5家店铺及各自商品；同时批次#102与对比面板仍在页面中，没有把第一批次替换或隐藏。

## 刷新与重新登录

`two_batches_after_relogin.png`来自新浏览器会话。两个文件批次、最新批次标识、默认勾选和店铺/商品对比全部恢复，说明视图依赖服务端持久化批次而非仅保存在前端内存。既有日报仍保持原始店铺和15/15状态。
