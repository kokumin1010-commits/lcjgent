# LCJ HR唯一主档与跨模块状态联动最终交付

**日期：** 2026-08-27

**作者：** Manus AI

**生产环境：** Railway MySQL

**旧TiDB：** 连接、读取、恢复均为0

## 1. 完成结果

本次修复把HR员工身份统一为单一规范主档。生产当前人员在HR、报告员工、招聘、个人早会、团队早会、达人BD与积分模块均为25人；HR页面人物目录25、现在活动25，报告员工页面25行且姓名行唯一。排班、店铺负责人、任务、问题追踪、聊天、TikTok竞品日报和月度同事奖励也统一采用当前规范HR主档条件。

| 生产检查 | 结果 |
|---|---:|
| HR当前主档 | 25 |
| 报告员工当前档案 | 25 |
| HR统一目录 | 25 |
| 招聘担当者 | 25 |
| 个人早会成员 | 25 |
| 团队早会成员 | 25 |
| 达人BD员工 | 25 |
| 积分当前员工 | 25 |
| 报告员工页面重复姓名行 | 0 |
| 浏览器console/page/request错误 | 0 |
| 最终只读回归业务写入 | 0 |

## 2. 根因与生产数据处理

生产审计发现`staff`主档曾有28个活动记录，而HR统一目录与报告员工仅有24人。三组记录可由“标准化姓名一致＋完全相同且已验证邮箱”共同证明为同一员工；招聘、早会与达人BD因此重复显示。积分模块还会同时显示当前档案与旧归档日报档案。

系统没有按姓名自动合并。三组重复均先进行跨表引用预览，确认冲突为0，再在加密备份保护下逐组执行原子事务：第一组没有业务引用；第二组迁移1条早会朗读引用；第三组迁移1个店铺负责人和5条排班引用。主档保留，副本以`mergedIntoStaffId`软合并归档，历史记录不删除。

合并后发现唯一活动HR主档已有一条归档`report_staff`关联。系统恢复原有日报员工档案，而不是新建第二条记录，因此最终所有模块统一为25人。

## 3. 永久防重复规则

| 规则 | 实现 |
|---|---|
| 身份判定 | 仅使用已验证标准化邮箱；同名不同邮箱绝不合并 |
| 新增员工 | 同一事务建立`staff`与`report_staff`；重复已验证邮箱直接拒绝 |
| 编辑邮箱 | 更新身份键并检查唯一性，禁止制造重复主档 |
| 报告员工关联 | 当前未归档`report_staff`对`staff`一对一唯一 |
| 合并操作 | 仅管理员、明确主档/副本ID、确认短语、2小时内专用加密备份 |
| 历史数据 | 日报、录音、排班、任务、交易等历史均保留并迁移引用，不删除事实记录 |
| 同名保护 | 名称只用于二次核对，不作为身份键；真实同名员工保持独立 |
| 合并副本 | 软归档且不可被HR恢复接口重新激活 |

## 4. HR变动如何传播

当前人员选择器统一要求：`isActive='active'`、`archivedAt IS NULL`、`mergedIntoStaffId IS NULL`。因此HR退职或归档后，该人员会从报告员工当前名单、排班新增、早会成员、招聘担当、达人BD负责人、积分发放与排行、店铺负责人选择、问题通知、聊天邀请、TikTok日报权限和月度同事奖励中同步移除。复职后，主档与原有关联日报员工档案会原子恢复，相关当前名单重新出现。

历史记录不跟随隐藏：员工过去的日报、录音、排班、任务、聊天和积分流水继续保留，避免审计或财务历史丢失。

## 5. 备份、部署与审计

| 阶段 | 证据 |
|---|---|
| schema部署前基线 | 已验证成功备份id 149 |
| 三组合并前 | 加密备份id 150，394表、113,958行、checksum存在 |
| 三组合并后 | 加密备份id 151，394表、113,970行、checksum存在 |
| 漏档原档恢复前 | 加密备份id 152，checksum存在 |
| 最终一致性后 | 加密备份id 153，`post-staff-identity-merge`，checksum存在 |
| 合并审计 | 3条success，均绑定backup 150 |
| Railway重启 | docs-only commit重启成功后所有模块仍为25 |

主要生产commit：`c6c9c1dd`（中央HR身份与跨模块过滤）、`c3ff01a4`（Railway发布窗口与显式备份门禁）、`585e3b4f`（恢复既有归档日报档案）、`9de007d5`（生产验证证据）。

首次`c6c9c1dd`的Railway release未切流，生产仍运行旧版本且业务写入为0。其原因是启动监听前设计了两次耗时全库备份；已改为启动只验证最近成功备份并快速建schema，真正合并前后由管理员显式备份。后续全部Railway发布成功。

## 6. 验证范围

部署前完成61/61静态规则、隔离合并事务、幂等、同名隔离、引用冲突、审计失败回滚、归档日报原档恢复不重复，以及18个服务文件定向构建。生产完成API关系图、跨模块人数、HR页面、报告员工页面、备份健康、审计状态和Railway重启后的再次只读回归。

> 当前HR主档是所有应用的人员来源；今后员工新增、修改、退职、恢复和归档均会由统一状态条件实时传播，不再依赖旧历史恢复任务。
