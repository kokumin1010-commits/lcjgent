# LCJ MALL Master数据分类与恢复规则（2026-08-26）

## 范围与不可变边界

本规则仅允许使用保存于`lcjgent_restore`的本地备份、Git历史、DB操作历史、用户提供材料与Railway MySQL只读审计结果。**旧Manus管理TiDB不得连接、读取、回滚或重新挂载。** 生产数据库仅为Railway MySQL。

任何页面为空时，不得用相似业务数据替代。只有存在直接行级证据或生产表中已存在真实行、但查询/筛选/关联断线时，才能恢复或修复显示。生产和保存备份均无记录时，只修复表结构、API或空状态，不创建虚构业务行。

## 账号与资料的正确分类

| 数据对象 | 正确归属 | 是否属于`platform_accounts`登录凭据 | 处理规则 |
|---|---|---:|---|
| LCJ后台用户 | `users`、`/master/system-users` | 否 | 维持独立认证与权限模型，不复制到平台账号。 |
| 主播登录 | `livers`及主播认证流程 | 否 | 主播邮箱、哈希、UID与社媒主页不得复制成外部平台凭据。 |
| LINE用户 | `line_users`及LINE会员功能 | 否 | OpenID/LINE用户资料不是外部平台登录密码。 |
| Festival账号 | `festival_accounts`及Festival专用页面 | 否 | 活动申请自动生成账号不得混入通用平台凭据。 |
| 品牌与Lark资料 | `brands` | 否 | TikTok Shop ID、Lark负责人、品牌阶段留在品牌模型。 |
| 品牌/客户/主播/申请者联系人 | `contact_info`或原始业务表 | 否 | 可作为CRM联系人展示，但不得显示密码字段。 |
| 主播TikTok/Instagram/YouTube主页 | `livers`社媒字段 | 否 | 主页URL/handle不是登录资格信息。 |
| Festival企业/主播申请SNS | Festival申请表 | 否 | 申请信息不能推断为账号凭据。 |
| 人工创建且非恢复投影的平台凭据 | `platform_accounts` | 是 | 仅此类记录默认显示于“平台登录凭据”；不得覆盖或删除。 |

`platform_accounts.notes`中带有`recovery_source=`标记的142条记录由恢复投影脚本生成，原保存备份中的`platform_accounts`为0行；因此这些记录不得作为原始登录账号展示。归档时只移动带明确恢复标记的记录，不触碰任何人工创建记录。

## 页面状态分类

| 分类 | 定义 | 实施 |
|---|---|---|
| A | 生产有真实数据且页面查询正常 | 保持，仅回归测试。 |
| B | 生产有数据，但月份、日期、关联、权限或查询映射导致页面空 | 优先修复查询与回退，不改写事实日期或业务数值。 |
| C | 生产为空，但保存备份/DB历史有直接可恢复行 | 前后强制加密备份；幂等、事务式恢复；验证行数与关键字段。 |
| D | 生产与保存证据均无业务行 | 不造数据；可补表结构、API及明确空状态。 |
| E | 路由、tRPC、表结构或组件断线 | 修复代码/结构，不自动生成业务行。 |
| F | 数据类型误分类 | 迁移到正确分类或隔离归档；停止所有再生成路径。 |

## 账号管理恒久修复规则

1. Lark同步继续维护`brands`，联系人投影继续维护`contact_info`；不再向`platform_accounts`写入品牌、店铺ID、主播SNS或Festival申请资料。
2. 启动恢复和每6小时同步均不得把非凭据资料重新生成成平台账号。
3. 现有带`recovery_source=`的错误投影必须在强制备份后事务式移动到只读归档表；人工平台凭据不移动、不覆盖、不删除。
4. `account.listAccounts`、单条读取、平台筛选、更新和删除均必须排除或拒绝访问恢复投影，形成服务端安全边界，而不是仅依赖UI隐藏。
5. UI标题明确为“平台登录凭据”；当无人工凭据时显示“没有已确认的登录凭据”，并提供`/master/system-users`、`/master/brands`、`/master/livers`、`/master/festival`导向，避免把真实认证模型复制到该页面。
6. 密码不写日志、不进入审计报告；不得显示任何由品牌/SNS资料推断的密码。

## 其他Master页面恢复优先级

第一优先修复存在生产数据但被过滤或关联隐藏的B类页面，例如任务与员工关联、销售检查月份回退、直播建议日期匹配、选品中心排期/采购时间回退。第二优先修复有保存备份直接证据的RBAC等配置表。第三优先修复E类表结构/API断线，使页面正确加载并展示真实空状态。D类页面不得插入模拟记录。

复审模型给出的结论不是自动事实。例如`accountRouter`实际已挂载于主tRPC路由，因此账号页的真实问题是**F类误分类**而非路由未注册；所有实施必须以源码和确定性检查为最终依据。
