# KG／KYOGOKU商品恢复实施日志（2026-08-26）

## 问题

用户指出选品中心商品管理仍不完整，并要求优先恢复KG品牌商品。生产基线为83条可见`selection_products`，其中已恢复KG主列表约35条。页面还在所有无子SKU父商品后追加“子SKUなし”行，造成截图中几十行重复空白。

## 根因

此前恢复重点是保存商品主档，未将收据/直播中的完整KG商品名和真实变体建成父子SKU。前端在父商品循环结束后再次遍历所有父商品，并为每个无子SKU商品强制渲染一条空行；后端分页同时包含父与子商品，未来加入子SKU后还会导致子SKU占用分页但被父列表过滤。

## 证据化修复

从12类本地证据包、保存收据/直播记录、Git历史、数据库操作历史和已有83件基线中确定：

| 分类 | 数量 | 写入方式 |
|---|---:|---|
| 新KG父商品 | 3 | `selection_products`，`offline`，无推测当前价格 |
| 真实子SKU | 10 | `selection_products.parentProductId`，`offline` |
| 保存历史价格 | 5 | MEGAガチャ袋A/B/C/D/S，写入历史最低价与价格历史 |
| 历史目录证据 | 40 | `historical_product_catalog`只读记录 |
| 已审图片记录 | 6 | 5个唯一官方无价格静态文件 |

官方页面只用于正式名称、品牌和完全同款图片核验。网页当前价格没有写入数据库；促销价格图被人工拒绝并替换为同一官方相册的无价格产品图。

## 代码修复

新增`server/kgProductRecovery.ts`，采用唯一来源键、身份冲突阻断、事务、结构化运行日志和Railway MySQL前后加密备份。模块只补充缺失字段，不覆盖人工价格、库存、佣金、状态、图片或条码。

`selectionCenter.getProducts`改为只按父商品分页，再附带本页父商品的真实子SKU；搜索可命中子SKU名、SKU代码和条码。商品页移除全局“子SKUなし”循环，只有真实子SKU存在时才显示折叠按钮。

## 部署前验证

`verify_kg_product_recovery_static.py`的全部检查通过：3个父商品、10个子SKU、40条历史目录、5条历史价格、6条图片审计、13个唯一来源键、全部父键存在、无网页当前价格、全部初始离线、无旧TiDB连接、分页与显示修复存在。服务端恢复模块、选品路由、服务启动入口和浏览器页面均通过目标级esbuild；全服务入口仅报告项目既有`sharp`导入警告，与本次修改无关。

## 首次生产尝试与热修复

首次部署已成功完成前置加密备份ID 52（333表、10,508行），随后mysql2拒绝证据对象中的`undefined`可选字段。事务完整回滚，生产仍为83件商品、0条本批证据商品，未发生部分写入。错误被结构化记录为`Bind parameters must not contain undefined`。

第一次热修复将全部8组事务SQL绑定值统一通过`sqlParams()`转换：`undefined`显式变为SQL `NULL`，保留原事务、身份冲突和健康验证。第二次生产尝试再次先完成加密备份ID 53（333表、10,511行），随后同样在事务内回滚，仍为83件、0条本批证据商品。

为消除无法定位的同文错误，第二次热修复加入公开版本标记`undefined-to-null-v2-staged`、所有查询参数清洗，以及父商品/子SKU/历史证据来源键级阶段日志。第三次尝试完成前置备份ID 55（333表、10,515行）后，精确定位为`kg-child-sku:98cba71077fa7121`（MEGA Aタイプ）的`sourceClass`为空，事务再次完整回滚。

清单生成器现为全部10个子SKU写入非空`sourceClass`和`sourceTable`：官方颜色3件、保存直播变体5件、保存收据变体2件；运行时同时增加非空兜底。静态检查新增`allChildrenHaveSourceClassification`并通过，新证据SHA为`e2a16baa9df16e0fcca1e4dcf70e1244e05e7dc413dff26f12a65e788948b250`。

## 待生产回填

热修复部署后回填成功运行状态、后置备份ID、父/子/历史/图片/价格计数、页面分块和5个静态图片的HTTP/MIME/SHA验证。
