# 選品中心・商品第二輪深掘り復元ルール

本復元は**旧Manus管理TiDBへ接続せず**、保存済みローカル資材、Git履歴、DB操作履歴、既存Railwayデータ、および商品同一性確認のための公開公式ページだけを使用する。Railway MySQLが唯一の本番データベースである。

| 分類 | 復元先 | 件数 | 取扱い |
| --- | --- | ---: | --- |
| 保存済み`brand_products`直接行 | `brand_products`および`selection_products` | 17件／主一覧16件 | 旧IDを保持。現行ブランドIDは保存ブランド目录で確認。価格・在庫・手数料は作らない。 |
| 保存済み`product_master`完全名称 | `selection_products` | 3件 | 主一覧ではオフライン。重複旧IDを含む9件は一意制約を壊さず、読み取り専用目录だけに保持。 |
| 保存済み`livestream_products`集計 | `selection_products`および読み取り専用目录 | 主一覧19件／目录20件 | 正確な保存名称・旧GMV・販売数を保持。現在価格・在庫には転用しない。 |
| 保存済みreceipt reviewサンプル | 読み取り専用目录 | 10件 | 途中で切れた名称は補完せず、`truncated_or_unknown`として保持。 |
| 公式同一商品画像 | 静的資産と画像監査表 | 19件 | 完全同一商品、HTTP 200、画像形式・寸法検証、目視確認の全条件を満たす画像だけを使用。 |

> **主商品一覧の復元結果は38件**で、すべて初期状態を`offline`とする。既存40件の価格、在庫、手数料、公開状態、画像を上書きしない。

公開ページの現在価格は、過去価格の直接証拠ではないためRailwayへ商品価格として書き込まない。セット数量が異なるページ、類似商品、二次流通だけで確認できる商品、検索結果のスクリーンショットは商品画像として採用しない。

保存DB履歴では`receipt_reviews`が2,000行、商品名の重複排除結果が1,626件だったことまでは確認できる。しかしローカルに行内容が残る商品名は10サンプルだけである。残り1,616件の名称、商品ID、価格、画像は復元不能であり、推測で生成しない。

復元は`selection-product-deep-v2`を安定キーとする幂等処理で行い、変更前後にAES-256-GCM暗号化バックアップを強制実行する。各商品は`productId`、証拠SHA-256、元テーブル、元ID、ブランド対応、画像SHA-256を監査表に保存する。
