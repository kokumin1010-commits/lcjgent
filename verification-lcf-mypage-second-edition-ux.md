# LCFマイページ 第2回開催回分離・同行者・共通アカウント検証

## 対象

第2回申込みがあるアカウントのマイページで、第1回の2026年9月8日・9日および八芳園の情報が表示される開催回混在を解消する。第2回を2026年12月8日・9日、東京都立産業貿易センター浜松町館2階展示室として表示し、本人による日程変更・理由付き申込取消、同行者別登録・入場QR、イベントガイド、統合ログイン、LCF/LCM共通パスワードメールを提供する。第1回の履歴、既存QR、受付、VIP、アフターパーティー、ブース予約履歴は維持する。

## 実装確認

| 項目 | 確認内容 |
|---|---|
| 開催回分離 | `shared/lcfEventDefinitions.ts` の `2026-02` を唯一の第2回日程・会場定義として使用した |
| 申込一覧 | 共通アカウントのメールに紐づく全開催回・全申込種別を返し、第2回だけを操作対象にした |
| 日程変更 | ライブコマーサー・一般参加は12月8日のみ、12月9日のみ、両日を本人が変更できる。企業・ブランドは両日固定とした |
| 申込取消 | 本人確認、開催回・申込ID所有権、5文字以上の理由、確定文言を必須化した。行は削除せず `cancelled` と理由を保持した |
| QR無効化 | 申込・同行者取消時は対象QRを `isActive=0` にし、受付処理でも無効QRを拒否する |
| 同行者 | 申込み単位で最大10名。氏名、フリガナ、メールを保持し、各人へ別Ticket IDを発行した |
| QR取り違え防止 | 開催回、申込種別、申込ID、保有者区分を照合し、本人と同行者のQRを分離した |
| 受付後の保護 | 受付済みの本人・同行者はマイページから変更・取消できず、運営対応へ案内する |
| マイページ | 第1回固定の申込詳細、八芳園イベント詳細、準備チェックリストを表示対象から外した |
| イベントガイド | 会場、本人・同行者QR、ブランド・商品、配信・マッチング・GMVの確認方法を第2回カード内に追加した |
| メニュー | `LCF参加・QR`、`LCMブランド・商品`、`LCM配信者プロフィール` と役割を明示した |
| ログイン | 通常ログインの既定遷移を役割別ワークスペースではなく `/lcf/mypage` に統一した |
| パスワードメール | 件名・本文を `LCF / LCM 共通アカウント` に変更し、イベント名・開催年固定を除去した |
| ブース予約 | 第1回の予約履歴は読み取り専用で保持。第2回は対象ブース・時間枠・開始日・上限の決定後に同方式で開放する案内へ変更した |

## 安全性

新テーブル `festival_application_companions` と既存 `lcf_tickets` の加算列だけを冪等に作成する。申込行、チケット行、同行者行を削除しない。本人操作はHttpOnly LCF共通アカウント認証、メール一致、開催回、申込種別、申込IDまたは同行者所有者IDで検証する。取消・日程変更・同行者操作は既存レート制限と活動ログを通す。

第1回ブース予約は同じ2026年内にあるが、第2回の未確定ルールと混在させず、履歴上は第1回キー `2026` に固定した。第2回ブース予約の書込み機能は今回追加していない。

## 自動検証

LCF・LCM関連34ファイル、229テストが成功した。専用回帰では第2回日程・会場、開催回所有権、取消履歴、同行者別QR、無効QR拒否、共通メニュー、統合ログイン、共通パスワードメール、第1回ブース履歴を固定した。

`pnpm run check` は既存の全体TypeScript負債によりexit 2だが、今回変更ファイルの新規診断は0件だった。`NODE_OPTIONS='--max-old-space-size=8192' pnpm run build` は成功した。ローカルDB未起動環境のため、回帰実行時の自動スキーマ初期化ログには既知の `ECONNREFUSED` が出るが、ソース契約テストはすべて成功している。

## 本番確認

機能コミット `9a634ae2` のGitHub checkとRailwayデプロイ成功後、既存の管理者兼一般参加アカウントで `/lcf/mypage` をGET-only確認した。通常アカウント画面へ表示され、管理画面へ自動転送されなかった。

本番では `LCF参加・QR` と `LCMブランド・商品` のメニュー、LCFとLCMの用途説明、第2回の「2026年12月8日（火）・9日（水）」「東京都立産業貿易センター浜松町館 2階展示室」、第2回参加ガイド、本人・同行者別QR説明、ブランド準備、配信者準備、マッチング・GMV説明が表示された。第1回の9月日程・八芳園・準備チェックリストは表示されなかった。

確認に使用した一般参加アカウントには第2回申込みも過去履歴もなかったため、役割に合う「企業・ブランドで申し込む」と安全な履歴空状態が表示された。実際の日程変更、申込取消、同行者追加・編集・取消、QR受付、パスワード変更メール送信は本番データを変更するため実施していない。これらは専用回帰とサーバー側の所有権・状態遷移検査で確認した。

本番認証セッションの管理ブラウザは `window.resizeTo(390, 844)` を実行しても `1280×1100` の固定表示を維持するため、同一セッションでのモバイル実機撮影はできなかった。新規セクションはモバイルを基準とした単列レイアウトとし、`sm` 以上でのみ複数列・横並びへ切り替える実装をソースとproduction buildで確認した。ユーザー提供の既存モバイル画面で問題となった旧日付・会場・チェックリストは、表示コンポーネント自体から除外した。
