# 店铺商品管理数据与安全规则

**日期：2026-08-26**

## 业务边界

店铺商品是某一店铺在平台上的销售上架信息，不等同于选品中心全局商品。`store_products.selectionProductId`只做可选关联；店铺的平台商品ID、SKU、库存、售价、推广和图片独立保存，不反向覆盖`selection_products`。

## 数据模型

| 表 | 责任 |
|---|---|
| `store_products` | 店铺、商品名、品牌、平台商品ID、内部SPU、分类、链接、原价、库存、状态、主图和选品关联 |
| `store_product_skus` | 商品下的SKU编码、平台SKU ID、条码、规格名、SKU图片、售价覆盖、库存和状态 |
| `store_product_images` | 商品或SKU的S3图片URL/key、MIME、文件大小、排序和主图标记 |
| `store_product_promotions` | 商品级推广开关、折扣类型、折扣值、原价快照、推广价、日期范围、渠道、状态和备注 |
| `store_product_audit_logs` | 新增、编辑、图片、SKU、推广、软删除与恢复的前后JSON和操作人 |

## 身份与去重

同一店铺内非空`platformProductId`必须唯一；同一店铺内非空`spuCode`必须唯一；同一商品内非空`skuCode`必须唯一。NULL不作为身份推断依据。删除采用`deletedAt`软删除，历史SKU、图片、推广和审计不物理删除。

## 价格与推广

商品`basePrice`为正常售价，不能为负数。推广类型支持`percentage`和`fixed_amount`。百分比折扣范围为0.01–100.00%，固定优惠必须大于0且不超过正常售价。推广价只由服务端计算：百分比为`basePrice × (1 - discount/100)`，固定优惠为`basePrice - discount`；JPY按1日元四舍五入且不得为负数。开始/结束时间均可为空，但结束时间不得早于开始时间。推广结束后不改写正常售价，历史推广记录继续保留。

第一版推广应用于商品级。SKU可独立维护售价和库存；若SKU未设置售价则继承商品正常售价。后续可在不改变现有表的前提下使用`store_product_promotions.skuId`扩展SKU级推广。

## 图片

图片字节只上传到S3/R2，MySQL只保存`url`与`key`。允许JPEG、PNG、WebP，单图最大8MB，每商品最多8张。移除图片只解除数据库引用，不实现底层对象物理删除。商品必须可以在无图状态保存，禁止用相似图、生成图或其他商品图片代替。

## 权限与审计

所有读取和写入均要求登录。写入记录操作人ID、名称和时间。商品、SKU、图片和推广不提供物理删除API；恢复软删除商品时保留原ID和全部历史。公开健康端点只返回表结构、计数、迁移和备份状态，不返回商品名、SKU、图片URL或价格。

## 迁移与备份

首次创建5张表前必须执行Railway加密前置备份；事务外DDL完成后验证表、索引与既有5店铺数量，再执行后置备份。迁移不从选品中心、CSV或店铺业绩自动生成商品。旧TiDB不得连接或使用。

## 界面

店铺详情顶部增加`业绩概览｜商品管理｜推广活动｜数据上传`导航。商品管理支持搜索、品牌、状态和推广筛选，显示主图、商品ID、SPU、SKU数、正常售价、推广价、折扣、库存和状态。创建/编辑抽屉可维护主档、SKU、多图和推广。推广活动页显示进行中、计划中、已结束和停用记录，不伪造GMV或推广效果。
