# 店铺负责人及人工资料持久性修复报告

**作者：Manus AI**

**日期：2026-08-26**

## 完成结论

`/master/store-management`中“负责人刚保存又消失”的问题已完成根因修复并上线。问题不是用户操作或浏览器缓存造成，而是旧GMV/HR启动恢复脚本在检测到店铺数量、名称或2026年7月GMV变化时重新执行破坏性恢复，把负责人、负责人ID、副负责人和备注写回旧截图基线，并替换用户上传的月度资料。

现在一次性恢复使用恢复标记中的稳定店铺ID判断完整性。店铺改名、GMV变化、新增店铺、商品迁移或部署重启不会再触发破坏性覆盖；真正缺失店铺需要补建时，已存在店铺只补空基础字段，人工负责人、头像、联系方式、备注和用户上传数据全部保留。[1]

| 验证项 | 生产结果 |
|---|---:|
| 活跃店铺 | 5 |
| 直接可确认负责人 | 1 |
| 当前头像 | 0 |
| 当前联系方式 | 0 |
| 人工资料保护 | 已启用 |
| 审计表 | 已创建 |
| 确定性检查 | 20/20 |
| 六域并行验证 | 6/6 |
| 旧TiDB | 未使用 |

## 为什么刚添加后消失

旧`restoreStores()`对已存在的五家店铺执行整行式恢复：`operatorName`被固定为旧截图值，`operatorId`、`operator2Id`和`operator2Name`被设为`NULL`，备注也被旧恢复说明覆盖。恢复完成检查使用可编辑的店铺名称和精确GMV，因此改名、上传新数据或相关表产生额外记录时，都可能被误判为“漂移”并在后续部署启动时重跑。

该路径已经移除。恢复模块不再删除`store_data_uploads`，不再清空负责人ID，不再以GMV或可编辑名称作为重跑条件，并允许系统中存在更多店铺。[1]

## 保存与审计升级

店铺创建、编辑和软删除现在全部在数据库事务中执行。选择HR人员作为负责人时，服务端根据`staff.id`重新读取姓名并同时保存ID与名称；输入自定义姓名时保存姓名并将ID置空；只有用户明确清空时才写入`NULL`。每次保存都会向`store_profile_audit_logs`写入变化字段、前后快照、操作人、来源和时间。

店铺编辑弹窗已新增“资料变更记录”和“启动恢复保护中”状态。负责人、头像、联系方式或备注保存后，可以在同一弹窗查看审计记录。[2]

## 可恢复边界与补录

保存证据只能直接确认`LCJチャンネル=王铸`。其他店铺本次刚填写后被覆盖的负责人、头像、联系方式、副负责人和人工备注没有进入可读取的本地快照或生产健康记录，因此没有猜测恢复。用户需要在升级后的编辑弹窗补录一次；此后的保存会进入审计，并在部署、GMV恢复、商品迁移和同步过程中保持。

## Railway备份与生产验证

`store-profile-v2-protect`在服务监听前完成。前置加密备份ID 69成功，包含341表、10,841行；后置加密备份ID 70成功，包含342表、10,842行。生产健康为`healthy=true`、`manualProfileProtection=true`、缺失表0。StoreManagement生产分块为HTTP 200，四个防覆盖/审计标记均已上线；失效静态资源继续返回404并禁止缓存。[2]

## 使用方法

进入店铺管理后，点击店铺卡右上角橙色编辑按钮，重新选择HR负责人或填写自定义负责人并保存。页面刷新后负责人应立即保留。再次打开编辑弹窗，可在底部“资料变更记录”确认变化字段、操作人和时间。头像、联系邮箱、电话和备注同样只需补录一次。

## References

[1]: https://github.com/kokumin1010-commits/lcjgent "lcjgent source and recovery implementation"
[2]: https://lcjmall.com/master/store-management "LCJ MALL Store Management"
