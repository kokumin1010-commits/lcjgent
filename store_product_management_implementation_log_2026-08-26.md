# 店铺商品管理实施日志

**日期：2026-08-26**

## 需求与基线

用户批准在`/master/store-management`每个店铺详情新增商品板块，登记商品信息、图片、平台商品ID、SPU、SKU、库存、是否推广和推广折扣。实施前生产店铺仍为5家；2026年7月GMV证据和2026年8月严格零值逻辑不得修改。仓库中不存在`store_products`、`store_product_skus`、`store_product_images`、`store_product_promotions`或`store_product_audit_logs`同名实现。

## 数据与安全设计

新增五张业务表及`store_product_upgrade_runs`运行表。店铺商品可选关联`selection_products`，但不会更新或复制全局选品主档。商品、SKU、图片引用与推广采用软删除/暂停，不提供物理删除API。所有写入记录操作人和前后JSON。商品图片只上传到S3/R2，MySQL只保存URL、key、MIME和大小。

首次结构升级由`runStoreProductUpgradeSetup()`在主服务监听前执行。它验证5家活动店铺，执行`pre-store-products-v1`加密备份，创建五张业务表，验证店铺与业务行数未改变，再执行`post-store-products-v1`备份。任何备份、DDL或计数验证失败都会阻止新实例上线。

## 后端能力

独立`storeProducts` tRPC路由提供16个受保护过程，覆盖列表、统计、详情、选品候选、商品新增/编辑/归档/恢复、SKU新增/编辑/归档、图片绑定/主图/移除、推广保存/暂停和推广列表。商品ID、SPU和SKU唯一性由数据库与可读冲突错误共同保护。

推广价仅由服务端计算。百分比折扣限制0.01–100%，固定优惠不得超过正常售价；JPY推广价按1日元四舍五入。推广停止不会覆盖商品正常售价。推广记录保留原价快照、推广价、日期、渠道、状态和操作人。

新增`/api/store-product-image-upload`受保护上传端点，只接受JPEG、PNG、WebP，单图最大8MB；数据库绑定另由审计tRPC完成。每商品最多8张图。

## 前端能力

店铺详情新增`业绩概览｜商品管理｜推广活动｜数据上传`导航。商品管理提供统计卡、搜索、品牌/状态/推广筛选、归档显示和商品表格。创建/编辑抽屉覆盖商品主档、选品关联、SKU、图片、推广预览和变更历史。推广活动页保留计划中、推广中、暂停和结束记录，并支持直接暂停。

## 部署前验证

确定性检查共24项，24/24通过。迁移、API、主路由、服务入口、商品工作区与店铺页面均通过目标级esbuild。六个独立风险域并行审计6/6完成；其中把既有`store_data_uploads`替换/删除功能误判为新商品模块物理删除的结论经人工剔除，因为该旧功能不在五张新商品表内且本次未调用。并行复核提出的SKU图片引用保存与推广列表直接暂停体验已补齐并重新通过验证。

## 部署状态

待Git提交、Railway监听前迁移、加密前后备份和生产分块/权限/健康复测后回填。
