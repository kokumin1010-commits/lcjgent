# LCF所有票券累计受付

- [x] 重新读取全栈维护规范，同步最新main并检查WORK_LOG与工作树。
- [x] 审计现有二维码编码、festival ticket表、checkIn/getTicketByCode、扫码与手动受付前端、统计和CSV。
- [x] 设计向后兼容的数据模型：旧二维码与票券ID不变，旧已受付票默认有效人数1。
- [x] 新增不可覆盖的逐次受付事件记录，包含票券、顺序、管理员、请求ID、时间与撤销状态。
- [x] 服务端使用数据库事务、行锁和唯一请求ID保证多管理员并发累计与网络重试幂等。
- [x] 所有企业、ライバー、一般参加票券统一允许累计受付，不设置人数硬上限；第10人起仅提示。
- [x] 扫码成功后暂停连续识别，必须明确点击「続けて1名受付」才能为同票再加1人。
- [x] 手动受付使用相同累计与幂等规则，显示「今回で○名目です」。
- [x] 后台列表增加受付人数、初回受付、最終受付，并提供二次确认的「直前の1名を取消」。
- [x] 撤销只标记最近有效事件并记录审计，不删除历史，不允许人数降到0以下。
- [x] 更新仪表板实际到场人数、已受付票券数与CSV字段，区分人数和票券数。
- [x] 增加旧票兼容、首次/重复、请求幂等、多管理员并发、连续误扫、撤销、权限、统计与CSV回归测试。
- [x] 运行完整生产构建、迁移静态审计、差分与敏感信息检查，不连接或修改生产数据。
- [ ] 提交GitHub main并确认CI、Railway迁移与生产部署成功。
- [ ] 生产只读验证旧二维码仍可识别、累计界面/统计/撤销入口存在且本轮写入0。
- [ ] 更新WORK_LOG并交付最终结果。
