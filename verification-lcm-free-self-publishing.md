# LCM無料セルフ登録・即時公開・事後モデレーション検証記録

## 変更方針

- ブランド・商品登録は当面無料と明記する。
- 新規LCM会員は利用条件への同意後、`approved` として直ちに利用開始する。
- 本人が新しく作成したブランドは、必須項目完成後に本人が `published` へ変更できる。
- 公開中ブランドの商品は、必須項目完成後に本人が `published` へ変更できる。
- 運営による事前審査は行わず、公開後に理由付きで会員停止、ブランド非公開、商品非公開を行う。
- 運営が停止した `rejected` / `suspended` / `archived` のブランド・商品は、本人操作で再公開できない。
- 既存LCFカタログ掲載ブランドとの連携だけは、なりすまし防止の管理権限確認を維持する。
- ライブコマーサー公式プロフィールの本人同意・運営確認と、取引確認済みレビューのモデレーションは維持する。

## データ確認

2026年9月17日に本番LCM管理画面をGET-onlyで確認した時点では、会員5件、ブランド2件、商品1件で、会員・ブランド・商品に旧 `pending` / `submitted` は0件だった。このため、既存行を一括更新するデータ移行は追加せず、新規登録と本人公開APIの状態遷移だけを変更した。

## 安全策

- 新規ブランドは作成者本人を `active owner` として保存する。
- 既存カタログブランドは `pending` の管理権限確認を維持する。
- 公開必須項目をサーバーで検証し、画面操作の迂回でも不完全なブランド・商品を公開できない。
- URLは既存のHTTPS・公式プラットフォームURL検証を維持する。
- 公開中レコードの編集時にも必須項目検証を行う。
- ブランド停止時は同ブランドの公開商品もトランザクション内で `suspended` にする。
- 会員・ブランド・商品の停止理由を必須とし、状態変更は監査ログへ保存する。
- 公開APIは引き続き `published` ブランド・商品だけを返す。
- 停止中レコードの再公開は運営操作だけに限定する。
- 第1回LCF掲載済みの会社名・ブランド名と一致する新規ブランド作成を拒否し、既存ブランド権限申請へ誘導する。
- 1アカウント50ブランド、1ブランド500商品を上限とし、事前審査なし運用での大量登録を抑止する。
- 同一アカウントによる同名ブランドの重複作成を拒否する。

## 自動検証

- 専用回帰と既存LCM主要回帰：5ファイル、42テスト成功。追加の悪用防止回帰を含む主要3ファイル、32テストも成功。
- LCF・LCM関連全回帰：41ファイル、285テスト成功。
- `pnpm run check` は既存TypeScript負債768件でexit 2。登録上限・なりすまし防止を含む今回変更ファイルの新規診断は0件。
- production build成功。既存の `sharp` import警告とローカルDB未起動によるmigration `ECONNREFUSED` は出たが、build成果物 `dist/index.js` と `dist/public/index.html` は正常生成された。

テスト中のDB接続拒否ログは、DB不要のソース契約テストが起動時スキーマ初期化を試行する既存挙動であり、テスト結果は全件成功している。

## ローカル目視確認

Vite単体プレビューの `/lcm` をデスクトップ幅で確認した。

- 役割別入口に「当面は登録無料」「無料でブランド登録を始める」が表示された。
- ブランド向け説明に「事前審査を待たずに自分で公開」と「問題時のみ運営が非公開・利用停止」が表示された。
- 五段階が「共通アカウント → ブランド下書き → 商品登録 → ブランド公開 → 商品公開」になった。
- LCF企業アカウントと新規共通アカウントの双方で、利用条件同意後すぐにブランド管理へ進める説明になった。
- 既存の商品検索、第一回出展商品特集、ライブコマーサーポップアップ導線は保全された。
- 390×844pxのモバイル幅で、ヒーロー、商品検索、公開件数、ログインCTAに横切れ・重なりがないことを確認した。

## 本番確認

機能コミット `6cf1b79c` について、GitHub check成功とRailwayデプロイ成功を確認した。

2026年9月17日に本番 `/lcm` をGET-onlyで確認し、次を確認した。

- 「当面は登録無料」「無料でブランド登録を始める」が表示された。
- ブランド向け五段階が、共通アカウント、ブランド下書き、商品登録、ブランド公開、商品公開の順で表示された。
- 「事前審査を待たずに自分で公開」「問題時のみ運営が非公開・利用停止」の説明が表示された。
- 公開商品1件、公開ブランド1件、第1回特集29件と、既存マーケット・特集分離が維持された。

本番 `/lcm/manage?workspace=brand` もGET-onlyで確認した。既存の安全なテスト会員セッションで「ブランド・商品登録は当面無料」が表示され、新しい会社・ブランドを本人が作成できる入口と、既存LCF掲載ブランドは管理権限確認を維持する説明が表示された。

本番 `/lcm/admin?tab=members` をGET-onlyで確認し、画面見出しが「事後モデレーション・申請・監査」、5会員が「利用中」と表示され、各会員に「利用案内メール再送」「会員をブロック」が表示された。旧 `pending` 会員は0件で、既存利用中会員の状態は変更されていない。

本番のブランド管理タブでは、公開中のCHEYENNEに「ブランドと公開商品を停止」、下書き中のDr.Kozuに「ブランド側が入力中・本人操作で公開可能」が表示された。商品管理タブでは、公開中商品に「商品を非公開」と「公開確認」が表示された。停止操作は実行していない。

本番データへの新規会員登録、ブランド作成、ブランド公開、商品公開、会員停止、ブランド停止、商品停止は行っていない。
