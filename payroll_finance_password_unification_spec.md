# 财务与工资明细密码统一规格

## 根因

外层财务管理与内层工资明细当前使用两个不同bcrypt摘要。用户通过外层财务密码后，在工资弹窗继续输入同一密码会被旧工资摘要拒绝，造成“两次输入却无法进入”。

## 修复规则

工资明细弹窗继续保留为敏感数据二次确认，但密码来源统一为财务管理密码摘要。服务端`financeAccess`提供唯一密码比较函数；`payrollAccess`不再保存或读取第二套密码摘要。前端文案明确要求输入“财务管理密码”，避免用户误认为另有工资密码。

外层财务页面仍必须先通过page-session绑定的财务Cookie；内层工资明细解锁成功后另发用户绑定的HttpOnly工资Cookie。工资查询与导入接口继续要求财务门禁和工资确认两者同时成立，不能通过直接调用接口绕过。

错误密码继续限流，正确密码清除当前失败计数。退出账号、重新锁定财务页面或重新打开财务页面时，工资解锁状态按现有安全规则清除。密码明文不得写入前端源码、URL、日志、数据库或Git。

## 验证

验证正确财务密码可打开工资明细，错误密码被拒绝，连续失败锁定，Cookie用户绑定与篡改拒绝，财务未解锁时不能调用工资解锁，刷新当前页面行为正常，重新锁定后工资接口再次拒绝。生产验证只读取工资页面结构和接口状态，不输出工资金额或员工工资明细，不修改财务数据。
