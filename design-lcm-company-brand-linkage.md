# LCM 会社・ブランド・商品連携 設計契約

## 目的

第1回LCF出展アーカイブには、会社名、ブランド名、商品名が同じカード内に混在している。新しい企業・ブランド会員が既存実績を見つけられず別ページを作ると、同一企業・同一ブランドが重複するため、ブランド管理の入口を**既存検索優先**へ変更する。

## データ構造

既存DBは変更せず、`lcm_brand_profiles.companyName`を会社、`lcm_brand_profiles.displayName`をブランド、`lcm_products`を商品として利用する。第1回アーカイブの原画像、旧URL、掲載テキスト、`sourceCatalogPage`は変更しない。

| 階層 | 現行の保存先 | 例 |
|---|---|---|
| 会社 | `lcm_brand_profiles.companyName` | アジア国際貿易株式会社 |
| ブランド | `lcm_brand_profiles.displayName` | CHEYENNE |
| 商品 | `lcm_products.name` | CHEYENNE520ブラックジンジャーバターコーヒー |

第1回カタログの検索用データは、各ページについて会社名・ブランド名・商品名を正規化した読取専用ディレクトリとして作る。元データの判別が曖昧なページは推測で確定せず、**掲載名**として表示し、運営承認時に会社名・ブランド名を確定する。

## 検索と連携申請

ブランド管理画面を開くと、中間の「同じアカウントでLCMを始める」ページを表示せず、最初に会社名・ブランド名・商品名の共通検索を表示する。

検索結果は会社単位でまとめ、その下にブランドと掲載商品を表示する。企業の正式担当者は「この会社と連携」、ブランド担当者・代理店は「このブランドと連携」を選べる。会社単位の申請は、同じ正規化会社名を持つ第1回カタログページを一括対象とする。ブランド単位の申請は選択したページだけを対象とする。

該当結果がない場合だけ「新しい会社・ブランドを登録」を表示する。新規ブランド作成時も会社名とブランド名を別入力とし、同じ正規化会社名・ブランド名または同じ`sourceCatalogPage`がある場合は重複作成を拒否する。

## 権限と承認

検索と既存アーカイブの閲覧はログイン後すぐ利用できるが、編集権限は自動付与しない。既存の`lcm_brand_members.status='pending'`を連携申請、`active`を承認済み権限として再利用する。

| 状態 | 利用可能な操作 |
|---|---|
| 未申請 | 検索、既存アーカイブ閲覧、新規作成 |
| 申請中 | 対象会社・ブランドの閲覧、審査状況確認 |
| 承認済み | 承認された会社またはブランドの商品・プロフィール編集 |
| 見送り・取消 | 編集不可。運営理由を表示し、必要に応じ再申請 |

会社単位申請は1回の操作で対象ブランドを一括申請し、運営側でも会社単位で一括承認・見送りできるようにする。申請者メール、会社名、対象ブランド、元カタログページ、申請時刻、審査者、審査結果を監査ログへ残す。

## 共通アカウント登録後の直接開始

共通登録で「企業・ブランド」を選んだ場合は、同じ登録操作内でLCM企業会員を初期化し、登録後すぐブランド検索・管理画面へ移動する。「ライブコマーサー」を選んだ場合もLCM会員を初期化し、直接ライブコマーサー画面へ移動する。

利用規約・プライバシー・登録情報を選択したワークスペースの初期設定へ利用する同意は共通登録画面へ統合する。イベント参加・情報閲覧を選んだ場合はLCM会員を作らず、イベント・QRマイページへ進める。共通アカウント作成だけではイベント申込、QR、予約、来場履歴を作成しない。

既存LCF会員でLCM利用同意が未記録の場合は、同意済みへ自動変更しない。従来の会社名・氏名再入力を含む大きな中間フォームと自動mutationは廃止し、確認済み表示名、利用目的、明示的な同意チェック、該当マイページを開くボタンだけの小さな利用確認を表示する。検索・連携申請・ブランド編集は、その確認後の同一マイページ内で開始する。

## パスワード

共通アカウント新規登録の最小長を12文字から6文字へ変更する。英字1文字以上、数字1文字以上、最大128文字の条件は維持し、フロント、サーバー、入力案内、エラー文、テストを同じ条件へ揃える。既存アカウントのパスワードは変更しない。

## 第2回初心者サポート

第2回ページには「初めてでも、会場から配信できる。」を中心に、初心者講習、ブランドとの調整、配信アカウント・商品設定、当日の配信準備サポートの4段階を掲載する。プラットフォーム審査、ブランド契約、配信開始、売上は保証しない。

## 検証契約

最低限、会社名・ブランド名・商品名検索、同一会社の複数ブランド表示、会社単位とブランド単位の申請、未承認編集拒否、運営一括承認、重複防止、既存アーカイブURL保持、6文字登録、英字・数字要件、登録後の直接遷移、イベント申込・QR非作成、第1回運用データ保全を回帰テストで固定する。
