# 用户截图对账事实（2026-08-30）

用户要求逐笔相加验证，并在筛选栏增加可点击月份选择。

截图一显示：筛选结果·支出金额为 **¥381,564.31 RMB**、**80件**，页面净额为 **-¥61,784.48 RMB**。

截图二显示筛选栏：类型为出金、账户为全部账户、日期范围为 **2026/06/01 至 2026/06/30**，右侧显示 **126件**。两个截图的法人选择或工资二次权限状态可能不同，不能仅凭截图把80件与126件判为同一范围；必须由服务器按同一筛选条件返回逐笔累计、原始记录数、工资受保护行数及最终零差异。

产品要求：在截图所示筛选栏直接提供月份选择；点击收入或支出汇总卡片可打开按金额从大到小排列的逐笔累计表，显示每笔金额、累计金额、原币总额和跨法人JPY参考值。工资未解锁时个人明细不能泄露，但工资总额和笔数必须完整计入。
