# LCF管理タブ・アカウント・第2回申込 設計契約

## 目的

`/lcf/admin`の各管理タブを共有・再読込可能なURLへ変更し、全アカウントの並べ替えと`/lcf/mypage`からの第2回申込を追加する。第1回の申込・QR・受付・VIP・アフターパーティー・予約履歴は変更しない。ライブコマーサーGMVランキングはユーザー指示により今回の対象外とし、既存データ・既存機能を変更しない。

## 現行監査

- LCF管理のメインタブは`mainTab`のローカルstateだけで管理され、URLへ反映されない。
- アカウントAPIは`id`、`accountType`、`createdAt`、`lastLoginAt`を全件返す。現状の表は取得順のままで、並べ替えUIはない。
- マイページの開催回履歴APIは、同一メールの企業・ライブコマーサー・一般申込を`eventYear`別に返す。第2回は`2026-02`として安全に判定できる。

## URL連動タブ

- URL形式は`/lcf/admin?tab=<key>`とする。
- 許可値は`dashboard`、`applications`、`event`、`sponsors`、`accounts`、`gmv`、`activity`、`checkin`、`booth`のみ。
- 未知値は`dashboard`として扱い、URLも安全な正規値へ置き換える。
- タブ操作は履歴を残し、ブラウザの戻る・進むで選択状態を復元する。
- 申込管理から特定アカウントへ移る場合は`?tab=accounts&email=<encoded>`を使い、メール値は画面内フィルター専用として扱う。

## アカウント並べ替え

- 初期値は「新規登録順」。登録日時の降順、同時刻はID降順とする。
- 選択肢は「新規登録順」「登録が古い順」「最終ログインが新しい順」「最終ログインが古い順」「会員種別」。
- 未ログインは最終ログイン順の末尾へ置く。
- 会員種別順は`admin`、`company`、`liver`、`general`、同種別内は登録日時降順とする。
- APIが返した全対象を絞り込み後に並べ替え、画面内の一部だけを並べ替えない。

## マイページ第2回申込

- `eventYear='2026-02'`の申込履歴を役割別に判定する。
- 単一ロールの企業・ブランド会員は`/lcf/apply/company?edition=2`、ライブコマーサー会員は`/lcf/apply/liver?edition=2`へ直接進む。
- 複数ロールは企業・ブランド／ライブコマーサーの2つのCTAを表示する。
- 一般参加会員には、企業・ブランドまたはライブコマーサーの申込種別を選べるCTAを表示する。
- 申込済みの役割は「申込済み」と申込状態を表示し、同じ役割の重複CTAを出さない。
- 既存会員のメール・パスワード認証とプロフィール引継ぎをそのまま使い、新しい会員テーブルや別ログインを作らない。
