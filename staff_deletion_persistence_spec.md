# LCJ员工删除持久化与历史恢复冻结规格

**日期：** 2026-08-27

**作者：** Manus AI

**适用环境：** LCJ MALL生产环境，仅Railway MySQL；旧Manus TiDB禁止连接、查询或恢复。

## 1. 已确认根因

报告员工的生产删除接口当前直接执行`DELETE FROM report_staff`，没有删除墓碑、操作者、删除时间或删除前快照。`runHr36DirectoryRecovery()`却在每次Railway启动时验证历史36人目录和关联报告员工数量；任何报告员工被删除后，固定历史数量不再满足，它会按2026-08-25证据重新创建缺失行并批量更新历史行。生产只读审计确认该任务在2026-08-27 07:29:18 UTC再次成功运行，执行了备份124/125，并把36个历史员工对应的报告员工更新时间统一改到07:29:13 UTC。

此外，HR“删除”实际采用可逆归档，只在`staff`写入`archivedAt`；报告员工列表当前不排除“已归档HR员工所关联的report_staff”，因此用户在HR隐藏人员后，仍会在报告员工页面看到同一人，容易表现为再次恢复。

`server/_core/index.ts`仍在每次生产启动时调用HR36历史人员恢复，GMV/五店混合恢复也包含创建`staff/report_staff`的副作用。用户已明确本次只修复HR与报告员工，早会、店铺、商品、主播、积分及其他业务模块均不得改变。

## 2. 权威顺序

| 优先级 | 数据事实 | 规则 |
|---:|---|---|
| 1 | 当前用户在管理页面进行的删除、归档、恢复、修改 | 最高权威，必须跨刷新、重新登录与Railway重启保持 |
| 2 | 当前Railway MySQL中的业务记录 | 作为唯一生产状态持续保存 |
| 3 | 历史恢复证据、Git快照、旧截图、加密备份 | 仅供只读审计；除非用户针对具体记录再次明确要求，否则不得自动回写 |
| 禁止 | 旧Manus TiDB | 不连接、不读取、不恢复 |

## 3. HR人员恢复冻结

生产启动只移除`runHr36DirectoryRecovery`的自动调用及导入，保留其只读health接口和历史源文件。`runGmvHrRecoveryOnce`继续运行五店与GMV恢复，但其事务不得再调用`restoreStaff`，员工数量也不得作为五店恢复成功条件；因此店铺模块行为保留、员工副作用停止。

选品价格套餐、主播首页财务、主播工资、LCJ Brain、账号品牌、报告/账号/商品、选品深度、KG商品、积分会员及会员业务引用等其他启动调用保持最新main原状。数据库加密备份scheduler、schema-only升级、提醒、邮件及正常业务scheduler继续运行。早会文件、路由、schema与UI不属于本修复，提交清单中必须为0。

## 4. 报告员工删除模型

`report_staff`增加`archivedAt`、`archivedBy`和`archiveReason`。管理页面的“删除”改为原子软删除：锁定目标行，保存删除前JSON，写入归档字段与`isActive='inactive'`，再写入`manual_data_change_events`的`archive`事件。不得删除日报、跟进、AI档案或任何历史引用。

所有现行报告员工列表必须排除`report_staff.archivedAt IS NOT NULL`。报告员工列表还必须排除其关联`staff.archivedAt IS NOT NULL`的人员，使HR归档在两个页面保持一致。归档行不得被普通更新、关联或自动匹配重新激活。

恢复必须是独立、显式、管理员操作，并写入`restore`事件；任何启动任务、健康检查或旧证据都不能调用恢复。

## 5. HR归档模型

继续使用现有`staff.archivedAt/archivedBy/archiveReason`和`hr_staff_archive_events`，不做物理删除。归档后，对应人员必须同时从HR可见目录、报告员工可见列表、在职员工列表与排班名簿中消失，但历史日报、任务、排班、工资与审计引用保持原ID。

只有用户在HR归档箱点击明确恢复时，才允许清除归档字段。旧历史恢复函数不得清除归档，也不得创建同名替代行。

## 6. 生产数据处理边界

当前报告员工物理删除没有写审计事件，因此不能仅凭现存名单猜测用户删除了哪些名字。可验证证据包括：HR归档事件、恢复任务前后加密备份差异，以及本次修复上线后新增的报告员工归档事件。若无法从证据唯一确定刚才删除的具体记录，只部署防复活修复，不自动删除任何可能仍需保留的员工；用户再次点击删除后，该操作将永久保持。

## 7. 必须通过的回归

| 场景 | 预期结果 |
|---|---|
| 删除报告员工后刷新 | 行保持隐藏，历史日报未删除 |
| 删除报告员工后重新登录 | 行保持隐藏 |
| 删除报告员工后连续两次执行生产启动流程 | 行保持隐藏；HR36调用次数为0，其他模块调用保持原状 |
| HR归档后打开报告员工页 | 同一关联人员不可见 |
| 归档后查询在职员工/排班 | 人员不再作为在职者出现 |
| 显式恢复 | 仅被指定ID恢复并记录操作者和前后快照 |
| 备份与scheduler | 保持healthy并继续运行 |
| 既有业务表 | 不新增测试员工、不删除日报、不改工资/店铺/商品/销售数据 |
| 其他模块文件 | 早会、店铺、商品、主播、积分等无本地差分 |
