# 店铺人工资料持久性规则

**作者：Manus AI**

**日期：2026-08-26**

## 保护范围

`managed_stores`中的店铺名称、平台、国家、链接、主负责人ID与名称、副负责人ID与名称、头像URL与对象key、联系邮箱、联系电话和备注均属于人工维护资料。任何GMV恢复、HR恢复、商品迁移、结构迁移、定时同步或部署重启均不得把这些字段覆盖为空、覆盖为旧截图值或根据可编辑名称重建。

## 恢复规则

GMV/HR证据恢复完成后，持久性判断必须使用恢复标记中保存的稳定店铺ID。负责人、店铺名称或GMV发生人工变化时，不得视为需要重跑一次性恢复。仅当稳定ID对应的证据店铺真实缺失时，恢复模块才可补建；对于已存在店铺，只能补充当前为空的基础字段，且不得删除或替换用户上传的`store_data_uploads`记录。

## 保存规则

负责人选择HR人员时，服务端必须根据`staff.id`重新读取姓名并同时保存ID和名称。自定义负责人必须保存`operatorId=NULL`和用户输入的名称。清空负责人必须由前端显式发送`NULL`；省略字段表示不修改。店铺资料创建、编辑和软删除必须在事务中同时写入审计记录。

## 审计规则

`store_profile_audit_logs`保存店铺ID、动作、变化字段、变更前后快照、操作人、来源和时间。审计表不向公共健康接口暴露具体资料；公共接口只返回审计条数、表是否存在和人工资料保护是否启用。头像底层对象不因资料变更审计或软删除而物理删除。

## 可恢复边界

保存证据只能直接确认`LCJチャンネル=王铸`。其他店铺本次刚填写后被覆盖的负责人、头像、联系方式、副负责人和人工备注没有进入可读取的保存快照，因此不得猜测恢复。恒久修复上线后，用户只需补录一次，后续保存会进入审计并在部署、同步和恢复过程中保持。
