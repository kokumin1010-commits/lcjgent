# 第2回LCF 事前マッチング・自己申告GMV管理設計

## 目的

前回の「公式LINEへスクリーンショット送付 → 運営が氏名・メーカーを照合 → スプレッドシートへ転記 → 手計算」を、第2回LCFの共通アカウント内で完結させる。TikTok APIによる売上自動取得は前提にせず、**本人の自己申告、非公開証拠画像、運営確認、監査可能な調整履歴**を唯一の確定経路とする。

## 対象開催回と対象商品

| 項目 | ルール |
|---|---|
| 開催回 | `eventYear = 2026-02` のみ |
| ライブコマーサー | 第2回ライブコマーサー申込が `confirmed` のLCF共通アカウント |
| 出展企業 | 第2回企業申込が `confirmed` のLCF共通アカウント |
| ブランド | 上記企業アカウントがactive権限を持つ正式連携ブランド |
| 商品 | 上記ブランドの `published` 商品 |
| 対象外 | 第1回アーカイブ商品、LCF未出展ブランド、未公開商品、却下・取消済み申込 |

第2回の申込が0件の期間は、候補を捏造・仮作成せず「出展ブランドの商品公開後に利用できます」と表示する。

## 事前マッチング

ライブコマーサーは、対象となる出展ブランドの公開商品を選び、希望内容と予定日を送信する。ブランド担当者は承認、追加確認、見送りを行う。運営は全件を閲覧できるが、通常の承認は当事者間で行う。

| 状態 | 意味 | 次に可能な操作 |
|---|---|---|
| `requested` | ライブコマーサーが依頼済み | ブランドが承認・追加確認・見送り |
| `needs_info` | ブランドが追加情報を依頼 | ライブコマーサーが追記・再送 |
| `approved` | 配信候補として合意 | 双方へ必要な連絡先を表示、GMV報告候補へ追加 |
| `declined` | ブランドが見送り | 理由表示、終了 |
| `cancelled` | ライブコマーサーが取消 | 終了 |
| `completed` | 配信・報告まで完了 | 履歴として保持 |

同一開催回・同一ライブコマーサー・同一商品で進行中の依頼は1件に限定する。商品名やメーカー名の自由入力は使わず、IDで固定する。連絡先は承認前に相手へ開示せず、承認後だけ申込時のLINE/Larkまたはメールを表示する。

## 自己申告GMV

ライブコマーサーが入力するのは、報告対象日、承認済みマッチング、自己申告GMV、任意の注文数・配信URL・メモ、証拠スクリーンショットである。本人名、受付日時、開催回、アカウントID、ブランド、商品はシステムが固定する。

| 状態 | ランキング反映 | 説明 |
|---|---:|---|
| `submitted` | しない | 本人が提出、運営未確認 |
| `needs_revision` | しない | 証拠不足または入力不一致で差戻し |
| `verified` | 反映する | 運営が画像と金額を確認 |
| `voided` | しない | 重複・誤報・取消として無効化 |

証拠画像はS3へ非公開キーで保存し、DBには公開URLではなくobject key、MIME、byte数、SHA-256を保存する。本人、対象ブランドのactive担当者、LCF管理者だけが1時間の署名URLを取得できる。公開ページ、SEO、CSVへ画像URLを出さない。

## 重複防止

同一アカウント・開催回・対象日・配信URLの進行中報告を一意にする。配信URLがない場合も、画像SHA-256の一致を警告し、運営が既存報告を確認してから処理する。重複や誤報は行を削除せず `voided` とし、理由、実行者、実行日時を履歴へ残す。

## 確定額と調整

`submittedAmount` は本人の自己申告額、`verifiedAmount` は運営確認後の基準額とする。取消・返金は元報告を書き換えず、符号付き調整行へ保存する。

> **確定GMV = verifiedAmount + 承認済み調整額の合計**

速報・アワードに反映するのは `verified` の報告だけで、画面上は必ず「運営確認済みの自己申告GMV」と表示する。注文数、未確認金額、申込数を混ぜない。

## 集計軸

| 集計 | ルール |
|---|---|
| 日別 | 第2回開催日ごとの確定GMV合計 |
| 2日間累計 | 12月8日・9日の確定GMV合計 |
| ライブコマーサー別 | festivalAccountId単位の確定GMV |
| ブランド別 | 承認済みマッチングのbrandProfileId単位 |
| 商品別 | 商品別証拠が確認できる報告だけ。推測配分しない |

ランキングの公開スイッチは初期OFFとし、まず管理画面だけに表示する。

## 画面

ライブコマーサーのマイページには「事前マッチング」「本日のGMVを報告」「提出履歴」を表示する。企業・ブランド側には「マッチング依頼」を表示する。LCF管理画面にはURL共有可能な `gmv` タブを追加し、同じ画面でマッチング監査、未確認件数、日別確認済みGMV、2日間累計、配信者別・ブランド別・商品別の内部集計、画像・申告額・確定額・担当者・調整履歴を表示する。

## 監査と禁止事項

運営の差戻し、無効化、返金調整、自己申告額と異なる金額での承認は理由必須で監査ログへ保存する。同額承認も実行者・日時・状態を監査ログへ残す。APIがない状態でTikTok売上を自動取得したと表現せず、OCRも実装・自動確定しない。未確認画像や自己申告中の値を公開ランキングへ出さない。第1回のGMV、申込、QR、受付、VIP、アフターパーティー、ブース予約を更新しない。

## 実装上の安全策

進行中マッチング、同日・同一配信URL、有効な証憑ハッシュにはDB一意キーを置き、事前照合だけでなく同時送信時にも重複を防止する。取消・無効化・差戻し後の再提出では有効キーだけを解放し、元レコードと元証憑は削除しない。差戻し再提出は新しい報告へ`supersedesReportId`を保存する。

証憑APIは`Cache-Control: no-store, private, max-age=0`とし、通常一覧ではprivate object keyとSHA-256を返さない。管理者画面もクリック時だけ1時間の署名URLを取得し、URLをDBや公開ページへ保存しない。
