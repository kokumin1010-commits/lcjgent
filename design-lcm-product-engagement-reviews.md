# LCM 商品エンゲージメント・出展実績・レビュー設計

## 公開名称と目的

公開名称は既存の **LCM｜ライブコマースマーケット** を維持する。Amazon型の分かりやすい情報階層を取り入れるが、注文・決済ECではなく、商品発見、興味登録、サンプル相談、卸商談、配信検討をつなぐB2Bマーケットとして設計する。

## 商品CTA

| 操作 | 対象 | 認証 | 重複・制限 |
|---|---|---|---|
| 興味あり | 公開中のブランド公式商品 | LCF・LCM共通アカウント | 1会員・1商品につき1件。再タップで解除 |
| サンプルカート追加 | `sampleAvailable=true`の公開商品 | LCF・LCM共通アカウント | 1会員・1商品につき1件。最大20商品 |
| サンプル請求 | `sampleAvailable=true`の公開商品 | 承認済みLCM会員 | 既存の処理中重複・月間上限を維持 |
| 取引条件確認・卸商談 | 公開中のブランド公式商品 | 承認済みLCM会員 | 既存の卸条件・最小発注数を維持 |
| 第1回アーカイブ閲覧 | 第1回LCF掲載ページ | 不要 | 現在申請可能と誤認するCTAは表示しない |

未認証者が「興味あり」「サンプル請求」「取引条件確認」を選んだ場合は、安全な相対return付きLCF・LCM共通ログインへ案内する。社内ログインへは送らない。

## サンプルカート

サンプルカートは購入・決済用ではなく、複数商品を比較してサンプル申請候補として保持する機能である。ヘッダーと商品詳細に現在件数を表示し、商品名、ブランド、定価、受付状態を一覧できる。受付停止・非公開になった商品は正式申請できず、理由を表示して削除できる。

正式申請時は、共通の配送先、利用目的、予定コンテンツ、予定日、メッセージを入力する。ただしDBには既存`lcm_sample_requests`へ商品ごとの独立申請として保存し、商品ごとに処理中重複、月間上限、公開状態、サンプル受付状態をサーバー側トランザクション内で再検証する。成功した商品だけをカートから除外し、一部失敗時は商品別結果を返す。初期上限は20商品とする。

## 開催回別出展タグ

出展実績はブランドが自由入力できない。運営が保有するカタログまたは開催回申込との照合結果だけを、開催回単位の実績として保持する。

第1回は`sourceCatalogPage`を根拠として、`2026-01`、`第1回LCF 出展実績`、`/livecommercefestival/2026/exhibitors`を登録する。タグは商品カード、商品詳細、ブランドページに表示し、該当開催回のアーカイブへリンクする。将来は同一ブランドに`2026-02`以降を複数行追加し、複数回出展を時系列で積み上げる。

## レビュー資格

レビューは実利用を確認できる会員だけが、1商品につき1件投稿できる。投稿資格は次の既存取引履歴からサーバー側で判定する。

| 確認根拠 | 対象状態 | 公開ラベル |
|---|---|---|
| サンプル申請 | `delivered`、`live_scheduled`、`completed` | サンプル受取確認済み |
| 卸商談 | `accepted`、`negotiating`、`completed` | 取引確認済み |

本人の自己申告だけでは投稿資格を与えない。ブランド自身の商品、未公開商品、第1回アーカイブだけの商品、確認対象外の取引状態では投稿できない。

## レビュー状態と公開

レビューは1〜5の整数評価、20〜2,000文字の本文、3〜120文字の見出しを持つ。初回投稿と公開後編集はいずれも`pending`となり、運営承認後だけ`published`になる。公開画面の平均評価・件数は`published`だけから計算し、0件時は星や仮件数を出さず「レビューはまだありません」と表示する。

投稿者名はLCM表示名を使用し、メール、配送先、電話、申請本文、卸条件などの個人情報・取引秘密は公開しない。投稿者は自分のレビューを非公開にできる。ブランドと会員は公開レビューを通報できるが、ブランドが直接削除または評価変更することはできない。

## 運営・ブランド対応

運営はレビュー本文、評価、投稿者種別、確認根拠、通報件数を確認し、公開、見送り、公開停止を行う。見送り・停止理由は必須とし、全操作を`lcm_audit_logs`へ記録する。

ブランド管理には自社商品のレビュー一覧と通報入口を表示する。ブランド返信は初期リリースでは実装せず、恣意的なレビュー操作を防ぐ。運営確認済みレビューだけを公開し、架空レビュー、初期レビュー、デモ評価、購入者を装う文言は作成しない。

## Amazon型の情報階層

商品詳細は、写真、ブランド・開催回タグ、商品名、定価、公開評価要約、興味あり、サンプル請求、取引条件、特徴、販売先、レビュー本文の順にする。購入ボタンは設けず、外部公式販売先またはB2B申請へ明確に分岐する。

商品一覧は比較速度を優先し、正方形の固定画像枠、ブランド1行、商品名2行、定価、開催回タグ、レビュー要約、主要CTAだけを表示する。デスクトップは5〜6列、タブレットは3〜4列、モバイルは2列とし、長い説明や大きな写真は商品詳細へ移す。

## データ保全

第1回LCFアーカイブ、既存商品URL、ブランド仮連携、公開審査、サンプル申請、卸商談の状態遷移は変更しない。新規テーブルは既存テーブルの削除・変更なしで追加し、起動時upgradeはDBロック、事前・事後バックアップ、既存件数不変確認を維持する。
