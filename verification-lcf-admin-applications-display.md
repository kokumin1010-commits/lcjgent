# LCF申込管理 全文表示・リンク・部署検索 検証

## 実装内容

申込管理一覧の固定幅・2行省略を廃止し、タブ別に十分な最小幅を持つ横スクロールテーブルへ変更した。長文は改行を保持して全文表示し、HTTPS URLを含む値だけを新しいタブで安全に開けるリンクとして表示する。HTTP URL、JavaScript URL、TikTokのアカウント名だけの文字列はリンク化しない。

企業申込と一般参加には、開催回ごとの実データから生成した部署フィルターを追加した。「部署：全て」「部署：未設定」と個別部署を選択でき、既存の全文検索、開催回、ステータス、アカウント有無と併用できる。ライブコマーサー申込は部署項目を持たないため部署フィルターを表示しない。

## 本番GET-only確認

機能コミット`6e1de9e5`のGitHub checkとRailway成功後、`/lcf/admin?tab=applications`を管理者セッションで開いた。第2回（2026年12月）は企業、ライブコマーサー、一般参加が0件の安全な空状態で、検索欄の「氏名・会社・部署・URL・来場目的などを検索...」、既存の開催回・ステータス・アカウント条件、企業タブの「部署：全て」、CSV出力を確認した。

第1回（2026年9月）へ切り替え、企業40件、ライブコマーサー466件、一般参加337件を保持していることを確認した。企業一覧ではブランド紹介の長文が省略されず、会社サイト、TikTok Shop URL、Lark招待URL、LINE短縮URLが実際のアンカー要素として表示された。HTTP URLやURLではないLINE/Lark IDは文字列のまま表示され、HTTPS以外を自動リンク化しない安全境界も維持した。

企業タブの部署フィルターを開き、「部署：全て」「部署：未設定」に加え、BizDev Division / Business Development Team、TikTok事業部、マーケティング部、ライブコマース本部、営業部、企画部、経営企画など、第1回企業申込に実在する部署候補が表示されることを確認した。

「マーケティング部」を選択すると、40件の元データから該当する株式会社ドクターイーストと株式会社Fewの2件だけが表示された。既存の開催回・状態・アカウント条件、CSV出力、横スクロールは同じ画面で維持された。

ライブコマーサータブでは部署フィルターを表示せず、466件を維持した。TikTok URL、LINE URL、自由記述を含むLark招待URLは全文表示され、HTTPS URLを含むものだけがアンカーになった。TikTokアカウント名、フォロワー数、LINE IDなどURLではない文字列はそのまま全文表示された。

一般参加タブでは337件を保持し、従来2行で省略されていた複数の来場目的が全文表示された。部署フィルターには「部署：未設定」に加え、EC事業者、MCN、ブランド、メーカー、メディア、広告代理店、物流企業など、一般参加申込の実データに基づく候補が表示された。

一覧は横スクロール可能で、各行の長文に応じて高さが広がる。既存の申込詳細、ステータス変更、受付状態、日程、アカウント管理、CSV出力には書込み操作を行っていない。
