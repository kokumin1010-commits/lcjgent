# LCF統合マイページ・既存会員簡略申込・ヒーロー構図 検証記録

## ローカル視覚確認

2026-09-16、フロントエンド検証環境で第2回ページを1440×900pxと390×844pxで撮影した。

| 画面 | 結果 |
|---|---|
| 第2回ページ・デスクトップ | ヒーロー高を920pxから760pxへ縮め、画像位置を下寄せしたことで、上部の天井空間が減り、黄色いLCF看板、会場の人流、見出し、2つの申込CTA、開催情報が900px高のファーストビュー内に収まった。 |
| 第2回ページ・モバイル | 見出し、説明、2つの申込CTA、開催情報の先頭が844px高の画面内に表示され、横はみ出しはない。黄色看板と来場者動線も見える。 |
| LCMヘッダー・デスクトップ | 旧「マイLCM」「共通マイページ」の2ボタンは表示されず、右端に「マイページ」1ボタンだけが表示される。商品・ライバー・アーカイブ導線は維持される。 |
| LCMヘッダー・モバイル | ロゴと「マイページ」1ボタンが同じ行に収まり、横はみ出しはない。省略された商品・ライバー導線は本文CTAから利用できる。 |

確認画像はリポジトリ外の`/home/ubuntu/webdev-static-assets/lcf-unified-mypage-verification/`に保存した。

## 認証・回帰確認

既存メールの存在確認は従来どおり真偽値だけを返し、既存会員と判定された場合だけパスワード画面へ進む。共通ログインと同じ認証処理が成功した後に限り、同一メール・同一申込種別・第1回`event_year='2026'`の最新プロフィールを返す。必須項目や電話・URL形式が現在の第2回入力条件を満たさない場合は自動引継ぎを行わず、通常入力へ切り替える。

パスワードは申込回答、チャット履歴、localStorageへ保存せず、サーバー側の既存パスワードハッシュも変更しない。第2回で既存アカウントのメールを使う申込は、同一メールの認証済みHttpOnly Cookieがない場合にサーバーで拒否する。第1回フォームと新規メールの公開申込は従来フローを維持する。

LCF関連回帰は26ファイル169件すべて成功した。production buildも成功した。全体TypeScript検査は既存負債で終了コード2だが、今回変更したファイルの抽出エラーは0件だった。build後のローカルDB migrationはDB未接続により継続ログを出す既存挙動であり、バンドル生成は完了している。

## 本番確認

コミット`3fb1dd97`のGitHub CIとRailway deployはいずれも成功した。`/2nd`、第2回企業・ライブコマーサー申込、`/lcf/mypage`、`/lcm`、ブランド・ライブコマーサー役割URL、第1回`/2026`はすべてHTTP 200だった。

本番`/2nd`を実ブラウザで確認し、縮小したヒーロー内に黄色看板、会場の人流、見出し、2申込CTA、開催情報がファーストビューで表示された。旧920px指定ではなく760px指定の配信bundleへ切り替わっている。

本番`/lcm`を未認証状態で確認し、ヘッダー右端は「マイページ」1ボタンだけになった。旧「マイLCM」「共通マイページ」は表示されず、「商品を探す」「ライバーを探す」「出展アーカイブ」は保持された。未認証時のマイページリンクは`/lcf/login?return=...`へ向き、ログイン後に元のLCMページへ戻れる。

本番の第2回企業申込は`1/17`でメールアドレスから開始し、「第1回で登録済みの会員様も、同じメールアドレスをご入力ください」と表示された。ここまでは参照のみで、申込データの作成は行っていない。

本番企業申込へ既存会員メールだけを入力すると`2/4`へ短縮され、会社名入力ではなく「会員様、ありがとうございます。第1回と同じパスワードを入力してください」と既存パスワード欄を表示した。「本人確認後、前回と同じ会社・担当者情報の再入力を省略できます」とパスワード再設定導線も表示された。パスワードは入力せず、ログイン・プロフィール取得・申込送信・QR発行・メール送信は行っていない。

本番の第2回ライブコマーサー申込は`1/12`でメールアドレスから開始し、企業申込と同じく既存会員へ同じメール入力を案内した。

本番ライブコマーサー申込へ既存会員メールだけを入力すると`2/5`へ短縮され、氏名入力ではなく既存パスワード欄を表示した。「本人確認後、前回と同じプロフィール情報の再入力を省略できます」と再設定導線も表示された。パスワードは入力せず、本番データの書込みは行っていない。

新規メールの分岐確認用に別セッションで企業申込`1/17`を開き、メール入力欄までの初期表示を確認した。申込送信は行わない。

未登録の検証用メールを入力すると`2/17`へ進み、パスワード欄ではなく従来どおり会社名入力を表示した。新規利用者の全項目フローが維持され、入力は会社名の手前で止めたため申込・会員・QR・メールの書込みはない。

既存の`/lcm/manage?workspace=brand`を未認証状態で開くと、`/lcf/login?return=%2Flcm%2Fmanage%3Fworkspace%3Dbrand...`へ遷移し、LCF・LCM共通ログインを表示した。既存役割URLと安全な復帰先が保持されている。ログイン操作は行っていない。
