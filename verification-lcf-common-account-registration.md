# LCF・LCM共通ログイン／申込不要アカウント登録 検証記録

## 実装内容

第2回LCFページとLCM公開ページの未認証会員入口を「ログイン」に統一した。認証済みの場合は、サーバーが申込・LCM会員・ブランド権限・ライブコマーサープロフィールを照合し、単一役割なら該当ワークスペース、企業・ブランドとライブコマーサーの複数役割なら統合マイページを既定遷移先として返す。明示的な安全な同一サイト内`return`がある場合は、その復帰先を優先する。

共通ログイン画面には「ログイン／新規登録」の切替を追加した。新規登録では「企業・ブランド」「ライブコマーサー」「イベント参加・情報閲覧」を選択でき、表示名、メールアドレス、英字・数字を含む12文字以上のパスワード、確認パスワード、プライバシー同意から共通アカウントだけを作成する。イベント申込、QR、予約、来場履歴は作成しない。

## 安全性

登録APIは厳格な入力検証、メール重複拒否、IP・メール単位のレート制限、パスワードハッシュ化、HttpOnly・Secure・SameSite=Lax cookie、監査ログを使用する。内部の既存`liver`キー、DB enum、API、URLは変更せず、公開文言だけを「ライブコマーサー」へ統一した。

## ローカル検証

| 検証項目 | 結果 |
|---|---|
| 共通ログイン画面のログイン／新規登録切替 | 成功 |
| 新規登録の3利用目的 | 成功 |
| 申込・QR非作成のソース回帰 | 成功 |
| 単一役割／複数役割の既定遷移 | 成功 |
| 安全な`return`優先 | 成功 |
| 未認証保護画面の共通ログイン直接遷移 | 成功 |
| 第2回ページの未認証「ログイン」表示 | 成功 |
| 390×844ログイン画面 | 成功 |
| 390×844新規登録画面 | 成功 |
| 390×844第2回ページ | 成功 |
| LCF・LCM関連回帰 | 29ファイル・195件成功 |
| LCM・共通ログイン専用回帰 | 6ファイル・50件成功 |
| production build | 成功。既存の`sharp`警告とローカルDB未接続によるmigration継続ログのみ |

全量TypeScript検査は既存の別領域の診断によりexit 2となったが、今回変更した共通認証、ログイン、第2回ページ、LCM公開・管理画面、認証ルーター、LCMルーターの抽出エラーは0件だった。最新main取り込み後のLCF・LCM関連回帰は29ファイル195件すべて成功し、production buildも再成功した。第2回ページの390×844表示では、未ログイン時の右上ボタンが「ログイン」となり、黄色いLCF看板、ヒーローコピー、2つの申込CTAが横にはみ出さず表示された。共通ログイン、新規登録、LCM公開ヘッダーもデスクトップ・モバイルで確認し、公開文言は「ライブコマーサー」に統一されている。

本番環境へテスト会員を作成してデータを汚さないため、登録送信は行わない。デプロイ後は画面・HTTP・GitHub CI・Railwayを読取専用で確認する。

## 本番反映

機能コミット`f168734b`を最新mainへpushし、GitHub CI run `35051103677`とRailway commit statusはいずれも成功した。`/lcf/login`、新規登録モード、`/2nd`、`/lcm`、ブランド・ライブコマーサー役割URL、第1回`/2026`はすべてHTTP 200だった。

実ブラウザで、共通ログインの「ログイン／新規登録」切替、3つの利用目的、12文字以上のパスワード案内、イベント申込・QRを自動作成しない説明、公開用語「ライブコマーサー」を確認した。第2回ページの未認証ヘッダーは「ログイン」で`/lcf/login`へ遷移し、LCM公開ページも「ログイン」1入口になった。未認証で`/lcm/manage?workspace=creator`を開くと、中間画面を出さず、安全なreturn付き共通ログインへ直接移動した。

役割別ログイン後遷移は純粋関数の実行回帰で、ブランドのみ、ライブコマーサーのみ、イベントのみ、複数役割の4条件を確認した。本番アカウント作成、ログイン、申込、QR発行、メール送信は実施していない。
