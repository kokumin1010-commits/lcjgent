# `/master` 账号分类与空页修复实施日志

## 目的与数据边界

本批修复只使用本地保存的恢复资产、Git历史、数据库操作历史、用户截图与Railway MySQL现状。**未连接、未回滚、未读取旧Manus管理TiDB**。没有直接证据的业务行不创建；品牌、TikTok Shop ID、主播SNS与Festival申请资料不再作为登录凭据。

## 修复前基线

| 项目 | 修复前状态 |
|---|---:|
| Railway表 | 319 |
| Railway总行数 | 4,254 |
| `platform_accounts` | 142 |
| `contact_info` | 507 |
| 带`recovery_source=`的平台账号投影 | 142（根因代码直接确认） |
| 原保存备份中的`platform_accounts` | 0 |
| `/master`真实基础路由 | 83 |
| 路由/选项卡目标 | 191 |
| 唯一选项卡 | 91 |

## 已实施代码修复

| 范围 | 实施内容 |
|---|---|
| 账号分类 | 移除品牌Shop ID、主播SNS、Festival企业/主播申请向`platform_accounts`生成种子的全部代码路径。 |
| 定时同步 | Lark启动同步与每6小时同步只更新品牌实体和CRM联系人，不再写平台账号。 |
| 无损迁移 | 部署启动时先强制执行AES-256-GCM加密备份，再把带`recovery_source=`的误投影完整复制到`platform_account_projection_archive`并从活动凭据表移除；人工记录不匹配该标记，不修改。 |
| API边界 | `account.listAccounts/getAccount/update/delete/getPlatforms`永久排除恢复投影记录。 |
| 账号UI | 明确分为“已确认的平台登录凭据”和“品牌・店铺・联系人（CRM）”，并链接系统用户、品牌、主播、Festival专用页面。 |
| 审计健康 | 新增密钥保护的只读账号分类健康端点，返回误投影计数、归档计数、恢复运行与前后备份状态，不返回凭据内容。 |
| Festival | 补上已有LINE数据面板的主选项卡入口；Check-in保持。 |
| 销售检查 | 初次打开自动选择数据库最新真实月份并标注来源，不改写业务日期。 |
| 主播详情 | 两套主播详情路由均按主播ID自动选择最新真实实绩月份；用户手动选择后不强制覆盖。 |
| 主播信用历史 | 当`liver_credits`没有持久历史时，从最新6个真实直播月份只读计算既有公式；不写入推导行。 |
| 直播建议 | 今日无排期时提供“查看最近真实排期日”入口；历史排期只读，AI生成与LINE发送仍限制为当天，防止误发送。 |
| 品牌排期 | 查询窗口从过去30天/未来60天扩大为过去2年/未来1年，显示保存的历史排期，不改写日期。 |
| 广告表单 | 对齐API与MySQL `plan` 枚举，加入幂等结构校正，使TikTok广告/直播SaaS真实提交可保存；不填充模拟申请。 |

## 审计纠错边界

并行审计中的以下候选经源码与Railway精确计数人工复核后判定为误报或真实空边界，因此未造数：员工任务已直接按`tasks.staffId`查询；Mega Channel端点已完整存在；产品补货请求端点已连接正确表；采购订单、聊天、问题、权限申请、步骤邮件等生产表真实为空且保存备份无直接行证据。此类页面保留明确空状态，等待真实操作产生数据。

## 提交前验证

| 验证 | 结果 |
|---|---|
| 16个修改入口的目标级esbuild转译 | 通过 |
| 完整服务端生产入口bundle | 通过（仅出现既有`sharp`导入警告） |
| `git diff --check` | 通过 |
| 全量TypeScript检查 | 项目规模在3.8 GiB沙箱内存中OOM，未生成代码错误输出 |
| 完整Vite构建 | 8,240模块转换完成，分块渲染阶段因沙箱内存上限终止；目标页面转译均通过 |

## 部署后结果

| 项目 | 生产结果 |
|---|---|
| 主修复提交 | `62a2b08bce660bacd6d37872979f0f6620ea9809` |
| 备份键热修复提交 | `df2be8afbc93bc8a347939290506863011a80623` |
| 恢复运行 | `success`（2026-08-26 03:48:16Z–03:48:30Z） |
| 误分类账号 | 147 → 0 |
| 只读归档 | 0 → 147 |
| 手工平台凭据 | 0（原保存备份同为0，未删除任何已确认凭据） |
| CRM联系人 | 521 |
| 品牌 | 186 |
| 前置加密备份 | `pre-acct-class-v2`，320表／4,403行，成功 |
| 后置加密备份 | `post-acct-class-v2`，320表／4,405行，成功 |
| 最新全商城审计 | 320表、101张非空、219张真实空、4,427行、查询失败0 |
| master URL复测 | 191/191 HTTP 200 |
| 当前静态资产 | 208/208 HTTP 200且Content-Type正确 |
| 失效asset策略 | HTTP 404、`Cache-Control: no-store`、`text/plain` |

首次部署时前置备份因新的`reason`字符串超出数据库列长度而在任何归档写入前失败，错误为`Data too long for column 'reason' at row 1`。随后将键缩短为`pre-acct-class-v2`／`post-acct-class-v2`并部署热修复；前后备份、147行归档和健康验证全部成功。该失败没有修改活动业务数据。

部署后再次确认，Lark同步处理188个品牌记录、更新521个联系人，但平台账号插入/更新均为0；因此启动同步和每6小时同步不会重新生成品牌/SNS“账号”。
